# Security Policy

## Reporting a vulnerability

**Do not open a public issue for a security vulnerability.**

Report privately through GitHub's
[private vulnerability reporting](https://github.com/pharanyx-labs/Horus/security/advisories/new)
on this repository. If that is unavailable to you, email **horus@pharanyx.co.uk**.

Please include: affected component and commit, a description of the security property that
is violated, reproduction steps or a proof-of-concept, and your assessment of impact.

**What to expect.** Acknowledgement within 7 days; an initial assessment within 14 days; and a
fix or a documented decision within 90 days for confirmed issues. Horus is maintained by a
single person as a research project; these are honest targets, not a commercial SLA.

Reporters are credited in the advisory and in `CHANGES.md` unless they prefer otherwise.
There is no bug-bounty programme.

---

## Current security posture: read this first

Horus is a **research microkernel**. It is not production-ready, has not been independently
audited by a third party, and has known unfixed security defects.

> ### The 2026-07 critical finding is fixed
>
> IPC endpoints and notifications used to be addressed by a raw integer index, with the
> `CAP_ENDPOINT.object` field never consulted, so any unprivileged program could intercept
> and forge messages to any userspace server. **Fixed 2026-07-27** ([C-1]/[C-2]): IPC is now
> capability-addressed, clients hold send-only capabilities, and every task has a private
> reply endpoint. The regression suite was falsified against the pre-fix kernel to confirm it
> detects the bug.

Horus remains **research-grade**. It has not been independently audited, no security-critical
change has ever been reviewed by a second person (**[C-5]**). Every security-specific CI job is
classified as merge-gating as of 2026-08-16, taking the ruleset from 22 required contexts toward
67, then to 71 with the three gates [G-8] added on 2026-08-17, then to 70 when
`smoke-kstack-park` was demoted to advisory that day for [G-9], and back up with
`smoke-exec-reenter` and `smoke-cr3-reclaim`, the gates for [G-9]'s exec component and [G-10]'s
page-table use-after-free, then to **73** with `smoke-spawn-owner`, the [G-11] gate added on
2026-08-18. The checked-in set is **74** as of 2026-08-19, with `doc-claims` (the gate that
derives every documented count and fails the build when a document disagrees) and the live
ruleset is one context behind it until `--sync-ruleset` is run, which is what "lags a merge"
means in practice. A scheduled `ruleset-audit` job now verifies the live ruleset against that
classification daily, as a GitHub App with `Administration: read`: the permission a workflow
token cannot be granted. **That App went live on 2026-08-19**: the scheduled run that morning
read the ruleset and reported `live ruleset 19007209 : 73 required contexts, matches`, where the
run 24 hours earlier had failed on the absent secrets. What keeps **[C-6]** open is now only the
other half (reconciliation is manual and lags by one merge) so it narrows again rather than
closing. **[I-7] closed on 2026-08-30**, and it closed in code rather than by reclassification. Its three
clauses -- "no retyping discipline, no per-task kernel-memory accounting, hard ceiling on system
size" -- are each answered. The retyping discipline landed 2026-07-27 for cspaces, endpoints and
notifications, and on 2026-08-30 for the TCB table, which was the last object class outside it.
The accounting is **S57**: creating a task carves the child's cspace from the caller's own
`CAP_UNTYPED`, and **S58** makes that budget subdividable, so "this task may create at most N
tasks" is expressible. The ceiling is gone: `g_max_tasks` is derived at boot from the reserve
that exists, `MAX_TASKS` is a provisioning constant nothing branches on, and the array that
would actually have capped it -- the revocation sweep's `cspace_desc_t spaces[MAX_TASKS + 1]`,
19% of a kernel stack at 256 tasks and an overflow at 2048 -- is allocated rather than on the
stack. The remaining scale parameters are listed in
[`docs/LIMITATIONS.md`](docs/LIMITATIONS.md) §3.1.

**The findings that remain are both process findings, and neither is closable by code**:
**[C-5]** (every security-critical path has been modified by one person) and **[C-6]**'s
remaining half (ruleset reconciliation lags a merge). **[G-9]** closed on 2026-08-21 (its last component was the claim auditor clearing
its own exemption before the release it exempts: a false positive of the checker, not a leak;
9/200 → 0/200 boots, mechanism proven 8/10 against 0/10 with the window widened in both arms) ,
are in [`docs/LIMITATIONS.md`](docs/LIMITATIONS.md). **[G-10]** closed on 2026-08-18: the
spawn/exec path's remaining process-wide state is serialised, and the two globals that could
wire a child's stdio from the wrong parent's cspace are parameters, so that inheritance is now
unexpressible rather than unlikely. Closing it surfaced **[G-11]**, also closed that day; the
armed program image was ambient state, and `SYS_SUDO` would elevate whatever was armed to uid 0
whether or not the authenticating task had staged it (**S21**).

**[G-8]** closed on 2026-08-17 in two parts, and it is the reason **S20** is in the table below.
A switch path published the outgoing task as claimable while the CPU making the switch was still
executing ISR frames on that task's kernel stack; a second CPU took it, resumed it to ring 3,
and its next trap rewrote those frames, memory corruption across a privilege boundary, and the
origin of every bogus resume `%rsp` the finding had recorded. The claim is now held until the
CPU has left the stack, and the property is checked on every interrupt rather than argued for.
Paired over 1600 boots: the pre-fix release site 31/800, the shipped one 0/800, Fisher p = 6.9 ×
10⁻¹⁰.

The finding's **second path** was closed the same day. When a task died and nothing else was
runnable, all three fault/exit fallbacks parked the CPU on `tasks[0].kernel_stack_top`: one
stack, shared by every CPU that took the path. A healthy session never enters it (0 parks in 3
boots), which is why it read as latent; on a workload that kills tasks it is entered 5–8 times
per boot and two CPUs were parked on that one stack 2–3 times per boot. Each CPU now parks on
its own. Closing it also required guarding the per-CPU idle stacks, which had **no guard page at
all**, so S9 below was overclaimed until 2026-08-17, independently of this finding, because
`enter_cpu_idle()` has always parked CPUs there.

Two closed on 2026-08-16. The write-ahead journal's missing `FLUSH CACHE` (**[I-10]**): so
filesystem crash-atomicity no longer depends on the emulator supplying durability the kernel
never asked for. And the bounded revocation closure (**[I-3]**), whose object-wide overflow
fallback let an unprivileged task force the kernel to destroy an unrelated peer's capability;
the closure is now exact at any subtree size.

Ambient `uid == 0` authority (**[I-1]**) was retired in stages: the syscall and object-store
gates on 2026-07-27, and the last one (the user database, which roadmap 0.2's sweep of
`syscall.c` never reached) on 2026-08-15 (**[H-1]**). See `docs/LIMITATIONS.md` §1.2.

Its appropriate uses are research, education, and development of the capability model
itself.

Every other known limitation is documented openly in
[`docs/LIMITATIONS.md`](docs/LIMITATIONS.md). Horus does not conceal its weaknesses; an
honest account of what is not enforced is more useful than a confident one that is wrong.

---

## Threat model

### Assets

| Asset | Protected by |
|---|---|
| Kernel integrity | Ring separation, SMEP/SMAP, W^X, `CR0.WP`, stack guards and canaries |
| Capability unforgeability | Slot-indexed cspaces; userspace never sees a capability struct |
| Inter-task isolation | Per-task page tables, software-validated user copies, flush-on-switch |
| Data at rest | Per-`(inode, block)` AEAD; keys never leave the kernel |
| Boot chain integrity | SHA-256 module manifest embedded in the kernel; TPM PCR 8/9 measurement |
| Volume key | Sealed under `PolicyPCR(8,9)`, a tampered boot cannot unseal it |
| User identity | Established only by `SYS_AUTH`; servers read it via `SYS_IPC_SENDER`, never from the client |

### Adversaries considered

**A1: Unprivileged ring-3 program.** Runs arbitrary code with a normal cspace. Wants kernel
compromise, another task's memory, another user's files, or privilege escalation. *Status:
defended for memory isolation, syscall authorisation, and: since 2026-07-27, IPC isolation.* A
task can reach a service only through a delegated capability naming that service's endpoint, and
a client capability confers send only. Not defended against denial of service (no CPU or
kernel-memory quotas). **Kernel-log forgery and eviction were open until 2026-08-20**
(**[H-2]**): `SYS_WRITE` fd 1 took no capability and appended unconditionally to the 16 KiB
`klog` that `SYS_DMESG` (which *does* require `CAP_KERNEL_LOG`) reads back, so any task could
forge lines indistinguishable from kernel diagnostics and flood the ring to evict genuine ones.
The append now requires `CAP_KERNEL_LOG` with the WRITE right (S23); the console write it rides
on stays ungated, because writing to a terminal is not the authority in question. See
`docs/LIMITATIONS.md` §1.6.

**Memory isolation was broken under SMP until 2026-08-17 (`[G-10]`), and the honest reading is
that this row was overclaimed.** `create_user_pagedir()` recycled a task slot's page tables
while another CPU still had them loaded in CR3: a CPU parked in the idle loop never reloads CR3,
and `SYS_KILL` marks a task dead while it is still executing in ring 3 on another core, after
which a spawn may take its slot. The freed frames went back to the physical pool and were handed
out as ordinary pages to other tasks while the first core was still translating through them,
which is a **cross-address-space read and write primitive available to any ring-3 task that can
get itself killed while running**, no capability required. It is fixed (the reclaim now refuses
to free an address space any CPU holds) and falsified with `CR3_RECLAIM_UNGUARDED=1` at 20
free-in-use boots in 20; witness `make smoke-cr3-reclaim`. Recorded here rather than only in
`LIMITATIONS.md` because it defeated the asset in the first row of the table above, and a threat
model that quietly omits the one time it failed is not a threat model.

**A1b: A ring-3 task holding a privileged *identity* but no capabilities.** A task at uid 0 with
an empty cspace. *Defended since 2026-08-15.* This adversary had no row here until **[H-1]** was
found, which is why the finding survived nineteen days of documents asserting it was closed: A1
is defined as an *unprivileged* ring-3 program, and the residual authority accrued to a
privileged-identity, unprivileged-cspace task that no adversary described.
`current_user_is_admin()` (`src/kernel/kusers.c`) now tests possession of `CAP_USER` and nothing
else. Witnessed twice: `smoke-captest` runs as uid 0 holding no `CAP_USER` and is refused
`useradd`/`userdel`/`passwd`-of-another; `smoke-session` drives the real shell and asserts root
may add a user while a standard user is refused.

**A1c: A ring-3 task that can stage a program image, against a task that can authenticate.**
*Defended since 2026-08-18* (**[G-11]**, **S21**). Neither task is privileged in A1's sense and
neither confuses a rights check: the attacker arms an image it is perfectly entitled to arm, and
the victim authenticates with its own correct password. The authority came from the *pairing*,
`SYS_SUDO` consumed whatever image happened to be armed, in a different syscall from the arm, so
the deputy elevated the attacker's program to uid 0 on the victim's credential. Recorded as its
own adversary for the reason A1b was: a defect that no described adversary could reach is a
defect nobody goes looking for. The armed image now carries the identity of the task that armed
it, and a consume by any other task is refused and audited.

**A2: Compromised userspace server.** `fs_server` or `console_server` under attacker control.
Confined to its own address space and its delegated capabilities; cannot reach kernel memory or
capabilities it was never granted. A compromised `fs_server` does control all filesystem policy,
and a compromised `console_server` sees all terminal traffic; both are in the TCB by
construction.

**A3: Offline storage attacker.** Has the disk image but not a measured-good boot. *Defended:*
AEAD encryption with a TPM-sealed KEK; rollback detection via a hierarchical MAC.

**A4: Boot-chain tamperer.** Modifies the kernel image or a boot module. *Defended:* module
hashes are verified against a manifest inside the kernel image, and both are measured into the
TPM; changed measurements leave the volume sealed. Verified adversarially in CI.

**A4b: Concurrent task exploiting kernel locking under SMP.** SMP is the default build, and the
spinlock's interrupt-nesting counter used to be a single global shared across CPUs (**[C-3]**),
so one CPU's lock release could re-enable interrupts while another still held a lock, admitting
a timer tick inside a critical section, the reentrancy the `cli`/`sti` discipline exists to
prevent. `spin_unlock` also issued an unconditional `sti`, which imposed `IF=1` on callers that
had deliberately masked interrupts and made boot-time interrupt enablement a consequence of the
locking bug (**[C-3.1]**). *Defended since 2026-08-11.* Both the nesting depth and the caller's
saved `RFLAGS.IF` are per-CPU (`irq_depth_pc[]` / `irq_saved_if_pc[]`,
`src/kernel/scheduler.c`), and the outermost release **restores** the caller's own `IF` rather
than asserting one. An equivalent patch was written on 2026-07-27 and reverted because the
ring-3 startup handshake depended on the accidental `sti`; what made the August attempt safe is
`preempt_on_tick`'s ring-0 guard, which means a ring-0 tick is never a switch point.
`IRQ_LEGACY_GLOBAL_LOCK=1` rebuilds the defect exactly, and `make smoke-irq-policy` gates
interrupt state at five named boot milestones, so the fix was measured rather than asserted. See
`docs/LIMITATIONS.md` §2.0 and `docs/ROADMAP.md` 1.1.

**A5: Local side-channel attacker.** Times another task's execution. *Partially defended:*
microarchitectural flush on task switch, SMT siblings parked, `CR4.TSD` denying ring-3 `RDTSC`.
Not defended against a concurrent sibling when SMT parking is disabled, or against shared L2/L3
channels.

**A6: Malicious contributor / build tamperer.** Attempts to introduce a backdoor via a pull
request or the CI pipeline. *Partially defended:* signed commits, protected branch with no
bypass actors, SHA-pinned actions, least-privilege `GITHUB_TOKEN`, reproducible builds verified
in CI, secret scanning with push protection. *Not defended:* **merges require no reviewer
approval.** See [Process security](#process-security).

### Explicitly out of scope

- **Physical attacks:** cold boot, bus probing, and DMA from a device the machine cannot
  confine. **DMA from a malicious PCI device is no longer wholly out of scope**: since
  2026-08-28 the kernel brings up Intel VT-d before any ring-3 task exists and gives every
  device an address space of its own that starts empty, so a device reaches exactly the frames
  its driver mapped (**S45**), and since 2026-08-29 those mappings do not outlive the
  capability that authorised them (**S53**). The scope limit is now the machine rather than the
  design: where there is no DMAR, `iommu_init` leaves `iommu_ready` at 0, says so on the wire,
  and every device reaches all of physical memory again. QEMU without `-device intel-iommu` is
  such a machine, which is why the gates that assert this property boot with `SMOKE_IOMMU=1`.
- **Firmware and hypervisor:** UEFI/BIOS and any underlying VMM are trusted.
- **Speculative-execution attacks** beyond the flush-on-switch and SMT-parking mitigations
  described above.
- **Denial of service by a local task.** There is no CPU or kernel-memory quota; a task can
  spin, allocate until the pool is exhausted, or force a broad capability revocation (see
  `docs/LIMITATIONS.md`).
- **Cryptographic primitives themselves.** Horus implements ChaCha20, SHA-256, BLAKE2b,
  Argon2, and an AEAD in `no_std` Rust. They are unaudited and not constant-time-verified.
  Treat them as research code.

---

## Security properties Horus attempts to provide

Stated as claims, with the mechanism and its witness, so each can be checked.

**This table is checked by CI.** `tools/check_invariants.py` (required job `invariants`, roadmap
4.12 / **[F-4.1]**) parses the rows below and fails the build if a property has no witness that
resolves to a real `make` target or CI job, if a witness names a target that does not exist or
that nothing runs, or if a control-arm flag named here is missing from `DEFECT_FLAGS` and so
would go unstamped at boot. The table **is** the registry; there is no parallel
`invariants.yaml`, because a second copy of these claims would drift from this one.
`.github/invariants.yml` holds exemptions only and is currently empty.

S16 carried an em-dash in its witness column until 2026-08-28, against enforcing code that was
real and called on every ring transition. That is what this check exists to make impossible.

| # | Claim | Mechanism | Witness |
|---|---|---|---|
| S1 | A task cannot exercise authority it holds no capability for | Central capability check in the syscall dispatch table, or, where the authorising capability is the one the CALLER NAMES, so a fixed table slot would authorise the wrong thing, a `cap_lookup` at a single resolver the handlers share (`ipc_ep_from_slot`, `ipc_notif_from_slot`, `iodev_from_slot`). Those entries are `SC_NONE`, and that is the gate rather than the absence of one | `make smoke-captest` (182 checks) |
| S2 | Delegation cannot widen authority | `rights & src->rights` in mint/grant/transfer | the `rust` job (`cargo test --manifest-path rust/Cargo.toml --release`), a required check with clippy `-D warnings` beside it |
| S3 | Revoking a capability revokes its entire derivation subtree, system-wide | `rust_cap_revoke_global` + subtree closure | the `rust` job, and `kani-bounded`, a required check whose proofs are unbounded over the capability algebra rather than over a sample |
| S4 | Revoking a capability does *not* revoke ancestors, siblings, or independent peers | Descendant-only closure | the `rust` job, and `kani-bounded`; the exactness proofs in particular, which is the half `--features=revoke_legacy_bounded` falsifies by restoring the object-wide overflow fallback |
| S5 | A revoked capability cannot be used, even from a stale snapshot | Structural nulling **and** serial-keyed generation bump | Rust unit tests, `smoke-captest` |
| S6 | An unknown or reserved syscall number cannot reach a handler | Table dispatch + compile-time size assertion | `_Static_assert`, `smoke-captest` |
| S7 | A user pointer cannot address kernel memory | Software page-walk requiring `PAGE_USER` | `smoke-captest`, SMEP/SMAP in `smoke-cpu` |
| S8 | No kernel page is simultaneously writable and executable | Per-section PTE permissions + `CR0.WP` | `make smoke-wx` (sweeps every leaf PTE) |
| S9 | A kernel stack overflow cannot reach adjacent memory | Unmapped guard page below every kernel stack: the 64 per-task stacks, the BSP boot stack, the boot and per-CPU IST fault stacks, and (since 2026-08-17) the per-CPU ring-0 idle/park stacks, which had none | `smoke-wx`, `smoke-wx-smp` (each family enumerated, guard absent and stack above it present) |
| S10 | A boot module that fails its hash check cannot be executed | Manifest verified at boot; read path refuses unverified | `make smoke-modules-tamper` |
| S11 | Tampering with the boot chain is detectable | TPM PCR 8/9 measurement | `make smoke-tpm`, `smoke-tpm-tamper` |
| S12 | A tampered boot cannot unlock the volume | KEK sealed under `PolicyPCR(8,9)` | `make smoke-tpm-seal` | **Since 2026-08-23 this property can be made mandatory**: `MEASURED_BOOT_REQUIRED=1` halts the machine when measured boot is unavailable, and refuses to unlock a persistent volume that was never sealed, closing the downgrade where a re-formatted, password-only disk made the requirement evaporate. Off by default; the ephemeral RAM vdisk is exempt (its key never outlives the boot). Witness `make smoke-measured-boot-required`, against `-control` (no TPM must halt) and `-volume-control` (an unsealed volume must be refused).
| S13 | A server can determine a client's true identity | `SYS_IPC_SENDER` returns the kernel-recorded uid, gated on the receive right | `smoke-fs-perms`, `smoke-captest` |
| S13a | A task can operate on an IPC object only via a capability naming it | Slot-resolved `ipc_ep_from_slot` / `ipc_notif_from_slot` | `make smoke-captest` (12 refusal checks) |
| S13b | A client cannot intercept or forge a server's replies | Clients minted WRITE-only; `recv`/`reply_to` need READ | `make smoke-captest` |
| S18 | Being uid 0 confers no kernel authority by itself | Every ambient `uid == 0` gate replaced by a typed capability | `make smoke-captest` (10 checks, run as uid 0) |
| S14 | File permissions are enforced against that identity | `fs_server` reference monitor | `make smoke-fs-perms` |
| S15 | An address-space slot rebuilt after task death leaks nothing | Cspace zeroed on reuse; page pool reclaimed | `make smoke-aspace` |
| S16 | A task cannot read another's XMM register file | `fpu_save` / `fpu_restore` (`src/kernel/scheduler.c`) capture and reinstate the whole 512-byte FXSAVE image (xmm0–15, the x87 stack, MXCSR) across every ring transition, from `interrupt_handler64`'s entry and exit paths. A task's registers are therefore never the physical leftovers of whoever ran before it. The image is per-task (`tcb_t.fpu_state`, 16-byte aligned because FXSAVE `#GP`s otherwise) and a new task starts from a template with MXCSR at its architectural default rather than a zeroed image; MXCSR 0 unmasks every SIMD exception, so the first ring-3 divide would trap | `make smoke-fpu`. **This row read `: ` until 2026-08-28**: the property was stated, the code enforcing it was real and called on every ring transition, and nothing bound the two together; the **[C-1]** shape exactly, and the first thing roadmap 4.12's invariant registry would have caught. The witness is two ring-3 tasks on one CPU: `fputest` loads a sentinel into all sixteen xmm registers and requires it intact after 64 switches away and back, `fpupeer` never writes an xmm register and requires that none of them ever holds that sentinel. Falsified one arm per half, and the halves are **separable** rather than two names for one gate: `FPU_NO_RESTORE=1` (`make smoke-fpu-leak-control`) discloses without losing: `FPUTEST: FAIL peer-saw-sentinel`, while the integrity check still passes, and `FPU_NO_SAVE=1` (`make smoke-fpu-save-control`) loses without disclosing: `FPUTEST: FAIL own-xmm-lost`, while the leak check still passes. Both 3 boots in 3; `smoke-fpu` goes red under each |
| S22 | A number stated in the documentation matches the tree it describes | `.github/doc-claims.yml` declares each derivable count and every place that states it; `tools/check_doc_claims.py` derives and compares, and rejects a declared claim whose pattern has stopped matching | `doc-claims`, a required check. Falsified three ways: a stale number, a reworded sentence that deletes a claim, and a retired phrasing reasserted |
| S17 | The shipped **kernel image** corresponds to the published source | Byte-for-byte reproducible build of `kernel.elf` | the `reproducible` CI job, a required check, which builds twice and diffs the recorded hash, and which since 2026-08-19 also runs `make smoke-repro-sha` (the record must cover every artifact) against `make smoke-repro-sha-control` (the pre-fix step, which records one of two and reports success). `make reproducible-build` builds **once** and records both artifacts in `.build.sha`. **`boot.iso` is deliberately not covered by this property**: grub-mkrescue stamps a wall-clock UUID into it, so it is not byte-reproducible, see `docs/LIMITATIONS.md` §5.3a. This row said "the shipped binary" until 2026-08-19, which read as the ISO |
| S19 | Audit-log history committed before a kernel compromise cannot be forged or rewritten | Forward-secure hash chain in `src/kernel/kaudit.c`: the MAC key is ratcheted one-way and erased after every entry | the `rust` job (`rust/src/audit.rs` unit tests) |
| S20 | A task's kernel stack is executed by at most one CPU at a time | Two paths. (a) The scheduler's claim on the outgoing task is held until the CPU has *left* its stack (released from `sched_release_deferred()`, called by `isr_common_stub64` after `movq %rax,%rsp`) with `g_kstack_inflight` halting the kernel if two CPUs are ever on one stack. (b) A CPU whose last runnable task dies parks on its **own** ring-0 stack, not on the one `tasks[0].kernel_stack_top` every CPU used to share; `sched_note_park()` halts if two ever pick the same one | `make smoke-kstack-race` and `smoke-kstack-park`, each with a control arm that restores the defect and must reproduce it. Since 2026-09-03 a **third** path, and the one that was actually still live: the FIRST-ENTRY path. `sched_enter_user()` claimed its task unconditionally while the launch site had already published it as schedulable, so an AP's timer tick landing in that window took the task and the entering CPU took it too -- two CPUs current on one task, neither impersonating, both on its single kernel stack (**[G-12]**, 0.31% of boots as measured before the checker fixes that preceded it). `enter_user_impl()` now re-validates under the scheduler lock and fails closed -- it will not enter a task another CPU holds, nor one that has stopped being schedulable, and parks with a report instead -- and `sched_publish_and_enter_user()` does the publish, the claim and `set_current_task()` in ONE acquisition of that lock, so there is no instant at which the task is schedulable and unclaimed. Witness `make smoke-enter-user-claim`, which holds the window open for a full spin budget and requires that nobody takes the task; falsified in two directions, `ENTER_USER_PUBLISH_EARLY=1` (`smoke-enter-user-claim-control`, the steal, 3 boots in 3) and `+ ENTER_USER_CLAIM_UNCHECKED=1` (`smoke-enter-user-collide-control`, the collision itself, 6 boots in 6), with the instrument `ENTER_USER_STEAL_WIDEN=1` set in all three arms. Base gate RED under the first arm; `make smoke-sched-invariants` red under the second. Since 2026-08-30 also `make smoke-task-ceiling`, which is about the WITNESS rather than the property: `g_kstack_inflight` was one `uint64_t` on the reasoning that `MAX_TASKS` was 64, so raising the ceiling to 256 made `1ULL << t` undefined above 63 and x86's 6-bit shift mask would have had the detector answer about task *t*-64 -- no report for a real collision above 63, a spurious one below it, and nothing anywhere saying so. Falsified in two directions, one per rule: `KSTACK_INFLIGHT_LEGACY_WORD=1` restores the single word and the alias pair (255, 191) shares a bit; `KSTACK_SLOT_INDEX_TRUNC=1` truncates the kernel-stack slot index and the pair shares a STACK, which is this property violated by construction rather than by race. Two arms because the first blinds the detector and the second creates the condition, and one arm covering both would not show the checks fail independently Since 2026-09-02 the detector's **identity** is witnessed too, by `make smoke-kstack-imp`: it asked `get_current_task()` -- `percpu_current_task[]`, which this system deliberately makes lie for the duration of every impersonation window -- so a CPU impersonating a peer that blocked on another core and had not been unwound off its stack yet reported two CPUs on one kernel stack while **neither CPU was on the other's**. A false positive in this witness, and the same blind spot `percpu_real_task[]` closed for the claim auditor, reached through a different caller; there is now one implementation of "who is this CPU really running", shared by both. The gate is **deterministic** -- the natural event ran at ~0.3% of boots at the time (**[G-12]**, since attributed and closed) -- and asserts both directions in one run: an impersonating CPU is not accused, and a genuine collision still is, because a predicate that never accuses anyone satisfies the first half alone. Falsified by `KSTACK_COLLIDE_IMPERSONATED=1` (`smoke-kstack-imp-control`), which restores the lying identity and requires the false accusation back; base gate RED under the same flag. |
| S25 | Which syscalls a test actually exercises is measured, not assumed | `SYSCALL_COVERAGE=1` records first entry into each handler body; `.github/syscall-coverage.yml` classifies all 93 implemented syscalls as covered or uncovered-with-a-reason; `tools/check_syscall_coverage.py` diffs the measured union against it. Currently **83 of 93**: the property is that the number is decided and stable, not that it holds any particular value. *(This sentence named a specific figure until 2026-08-30, and the figure it named was not even the one beside it -- a property stated as an equality against a stale constant, in the row whose whole point is that the constant is not the property.)* *(It read 76 until 2026-08-22, and the correction is not a regression: three syscalls with real handlers: `SYS_CAP_MINT`, `SYS_CAP_TRANSFER`, `SYS_CAP_MOVE`: sat in the dispatch table as bare numeric literals, and the derivation reads `[SYS_NAME] = { handler`, so it could not see them. They were unclassified and unmeasured, and nothing could fail on it. Naming them made them countable.)* *(It read 81 until 2026-08-23, and that correction went **both ways**. The deriver read the table as flat text, so three entries compiled only under a defect arm or a selftest flag (`SYS_OPEN`, `SYS_PREEMPT_TRACE`, `SYS_IRQ_POLICY_INFO`) counted as shipped; and seven more were still bare numeric literals, five of them live in the ship build. So the figure described no kernel that has ever booted. `scan_table` now evaluates the preprocessor, guarded entries are declared under `conditional:` with the flag that guards them, and a bare numeric entry is refused outright. The same blind spot had been recorded in prose beside `SYS_OPEN` since 2026-08-22, naming the wrong three, since two of the three it named were invisible to the regex that was supposed to be overcounting them. An unenforced note is worth that much.)* | `syscall-coverage`, a required check (`make smoke-syscall-coverage`). Falsified eleven ways: an unclassified syscall, a covered one that stopped being entered, an uncovered one that started being entered, a serial log carrying no `SYSCOV` lines at all, and (added 2026-08-23, one arm per new rule) a guarded entry becoming unconditional, a guarded entry nobody declared, a `conditional:` naming the wrong flag, a guarded syscall declared covered, a bare numeric index, an `#if` form the deriver cannot evaluate, and a `SYSCOV` number no active entry claims. Since 2026-08-30 the *promotions* are falsified too, which is a different question from the checker's rules: `SYSCOV_PROBES_ABSENT=1` (`make smoke-syscall-coverage-control`) removes `captest`'s section 13 and requires this gate to redden naming **exactly** the twelve `SC_NONE` handlers those probes enter, so a promotion that was free all along cannot pass as one the probes earned |
| S24 | The kernel never reads or writes a user address the caller did not name | Argument registers are 64-bit, and every pointer-taking wrapper in `include/syscall.h` hands its pointer over at full width — through `SYSCALL_UPTR()` or an explicit 64-bit cast, which `tools/check_syscall_abi.py` accepts equally because they expand identically; `user_copy` then refuses an address the task does not already have mapped rather than materialising one. **The two spellings are not equally falsifiable, and that is worth knowing rather than tidying away:** `SYSCALL_PTR_TRUNC32=1` reintroduces issue #176 by redefining `SYSCALL_UPTR` alone, so the runtime arm reaches only the wrappers spelled that way — 5 of the 52 pointer arguments the build-time check covers. A wrapper is moved into the arm when something starts calling it with a static buffer, which is how `sys_read_audit` came in on 2026-09-01 | `tools/check_syscall_abi.py`, a required check (`syscall-abi`), falsified two ways: a narrowed pointer in one wrapper, and a narrowed `SYSCALL_UPTR` definition. Runtime arms: `make smoke-klog-forge` reads the kernel log into a buffer above 4 GiB and `make smoke-auditprobe` does the same for the audit digest and the audit records — the sibling wrapper #176 was also about, which nothing had ever called — against `make smoke-klog-forge-abi-control` and `make smoke-auditprobe-control` (`SYSCALL_PTR_TRUNC32=1`), 3 boots in 3 each |
| S23 | A ring-3 task cannot forge entries into the kernel message ring, nor evict what is already there | `h_write` resolves `CAP_KERNEL_LOG` + `CAP_RIGHT_WRITE` through `cap_lookup` (type checked) and passes the result to `print_from_user`; without it the bytes reach the console and not `klog`. `root_cnode[15]` mints that capability READ-only and delegation may only narrow, so no task can currently hold the write right at all | `make smoke-klog-forge`, with `smoke-klog-forge-control` (`KLOG_WRITE_UNGATED=1`) reporting `FAIL forged+evicted` on every boot, 3 in 3 |
| S29 | Reaching a mounted subtree requires the capability for that mount, not the path | The VFS is a per-task library (`userspace/hvfs.c`), not a server, so no privileged intermediary holds capabilities to every backing filesystem. `hvfs_mount` refuses a slot holding no usable capability, and `hvfs_rpc` sends on the mount's own slot, an empty or wrong-type slot fails the kernel's IPC gate. **The prefix is a name, not a boundary**: a task holding a server's capability reaches all of that server wherever it is mounted; confinement is the server's job | `make smoke-vfs`: two servers, two mounts, 14 checks, **and since 2026-08-23 the ship build's own clients**: `posix.c` and `shell.c` walk every path through this library, so `smoke-newlib`, `smoke-session` and the coreutils gates exercise it too. `..` pops the walker's own descent stack rather than asking the server, which both removes a round trip and removes a thing a compromised server could lie about. Falsified by `smoke-newlib-walk-control` (the private walker restored) and `smoke-newlib-dotdot-control` (`..` asked of the server, which creates no such entry; the branch as it shipped in #195, dead on arrival), and by `smoke-vfs-mount-control` (`VFS_MOUNT_UNGATED=1`, a prefix alone installs a mount) and `smoke-vfs-prefix-control` (`VFS_FIRST_MATCH=1`, `/dev/zero` is answered by the wrong server) |
| S28 | A gate satisfied by a capability every task already holds is not a gate | The in-kernel ramfs surface (`SYS_OPEN`, ramfs create/list, and `SYS_READ` for fd ≥ 3) authorised on cspace slot 3 with `SC_ANYTYPE`, which the legacy `CAP_FRAME` in every task satisfies. All four are retired (**[H-3]**, 2026-08-22); an absent dispatch entry fails closed at `SYS_ERR_NOSYS` | `make smoke-passwd-probe`: an ordinary uid-1000 task holding no delegated capability is refused all four; **a sixth followed on 2026-08-24**: `SYS_GET_LINE` declares `SC_NONE`, so its handler was the only gate, and it accepted an **untyped** `cap_lookup` on slot 3 as a fallback to `CAP_CONSOLE`. That is console **input**, what the user is typing. Measured before the fix: captest, holding the decoy and no `CAP_CONSOLE`, passed the check and **blocked inside the read**, which is a stronger statement than being eligible. It survived because a ring-3 console server owns the UART in any live boot and `console_hw_owned()` refuses first, a *circumstance*, not a gate, the same shape as S30's boot ordering. Falsified by `smoke-captest-getline-control` (`GETLINE_SLOT3_FALLBACK=1`), which asserts a **stall**: a task admitted to a console read prints nothing, so absence is the evidence. **And, since 2026-08-23, a fifth**: `SYS_EXEC_LEGACY` (14) wore the same slot-3 authority and **created a task**. Measured from that probe before removal: it returned task id 2 to a uid-1000 caller. It survived the [H-3] sweep because its dispatch entry was written `[14]`, a bare number matching none of the `[SYS_NAME]` patterns that sweep, the coverage manifest, and every audit grep are built on; naming it in #201 is what made it visible. `create_task` assigns no uid, so the task it made carried whatever the slot held. Falsified by `smoke-passwd-probe-legacy-control` (`LEGACY_SYSCALLS_PRESENT=1`), under which the probe is handed a task id again. Falsified by `smoke-passwd-probe-control` (`RAMFS_SLOT3_GATE=1`), under which it opens a seeded file, reads bytes out of two more, creates a file and lists the store. *(Until 2026-08-22 the first of those was the file the user database was written into; that path has since been deleted as code that never ran: see `docs/LIMITATIONS.md` §2.6. The property is unchanged: it was never about what sat behind the gates.)* |
| S26 | Memory can only be mapped through a capability naming a kernel-managed frame object | `CAP_FRAME.object` is an INDEX into the frame table `SYS_RETYPE` populates, bounds-checked against `[DYN_FRAME_BASE, FRAME_INDEX_MAX)`, never a caller-supplied physical address. This is what makes the legacy `CAP_FRAME` every task is born holding in slot 3 (object `USER_AREA_BASE`, `READ\|WRITE\|EXEC`, and the capability that made [C-1] reachable) map nothing at all | `make smoke-frame`, with `smoke-frame-index-control` (`FRAME_INDEX_UNCHECKED=1`, which makes the object a physical address) mapping physical `0x400000` into ring 3 from that legacy capability on every boot |
| S27 | A memory mapping conveys no authority the capability does not carry | `SYS_MAP_FRAME` resolves the caller-named slot with `cap_lookup(slot, rights)`, so the capability must hold at least every right requested, and the PTE is built from `cap->rights & requested`. `SYS_CAP_MINT` is how a holder narrows a copy before delegating it: the only rights-reducing operation ring 3 has, since `SYS_CAP_GRANT` copies the source's rights whole | `make smoke-frame`: a `READ`-only delegate sees the sharer's bytes and cannot obtain a writable mapping. Falsified by `smoke-frame-rights-control` (`FRAME_RIGHTS_UNCHECKED=1`), under which its write lands |
| S21 | A program image can only be spawned by the task that armed it, and a child inherits its stdio from that same task | `loader_arm_commit()` records the arming task; `do_spawn` and `h_sudo` refuse a foreign or unowned image; the spawner's identity is a parameter of `wire_child_stdio` rather than a global | `make smoke-spawn-owner`, with `smoke-spawn-owner-control` (`SPAWN_OWNER_UNCHECKED=1`) spawning the foreign image on every boot |
| S30 | The CSPRNG never emits output before it is seeded from real entropy | `RngState::fill` (`rust/src/rng.rs`) returns false and zeroes the caller's buffer while `seeded` is false, instead of running ChaCha20 under the hardcoded startup key in `RngState::new()`, a **published** constant, since the build is reproducible. The two C wrappers (`secure_random_bytes`, `secure_random_u64`) halt on a refusal, the same way `entropy_init` already halted on a pool that did not take, and `rust_rng_u64()`: which returned a `uint64_t` and so had nowhere to put a refusal, was replaced by `rust_rng_u64_checked(uint64_t *)`. **This was previously true by boot ordering rather than by construction**: `entropy_init()` runs before the first consumer, which is a fact about one call site, not a property of the RNG | `make smoke-rng-seed`: a probe asks the pool for output before `entropy_init()` and must be refused, in a boot that must still reach the ring-3 shell banner. Falsified three ways: `smoke-rng-seed-control` (`RNG_UNSEEDED_LEGACY=1`, the check compiled out of `fill`) serves keystream and reddens the base gate; the Rust arm `cargo test --features rng_unseeded_legacy` fails `rng_refuses_before_seeding` and only that test; and an `if true` mutation making `fill` refuse *everything* reddens the base gate too, on the boot half rather than the marker |
| S31 | The capability algebra's authority invariants are proved over the whole input space, and the proofs run | Fifteen `#[kani::proof]` harnesses; `.github/kani-harnesses.yml` classifies each as gating or excused-with-a-reason, and `tools/check_kani_harnesses.py` fails the build on one in neither. Proved for **every** input: mint and grant yield exactly `requested & source`; lookup succeeds **exactly when** the capability holds every requested right; grant records its grantor as parent with a fresh derived serial, refuses an invalid source without writing anything, and is bounded by the destination cspace; revocation nulls exactly the target's subtree: no ancestors, all descendants. **The proofs did not gate anything until 2026-08-23**: the `kani` job is `workflow_dispatch`-only *and* carries `continue-on-error` on both steps, so no harness could redden a build: the `smoke-kstack-park` shape (required and unfailable at once) applied to formal verification | `kani-bounded`, a required check: eleven harnesses, **319 s** measured, no `continue-on-error`. Each new proof was falsified by mutating the property it claims, weakening lookup's rights test to "any overlap", dropping grant's `& src.rights`, zeroing the recorded parent, removing a bound, and confirming `VERIFICATION:- FAILED`. The four excused harnesses (the serial-keyed lineage pair and the two ELF validators) run in the manual `kani` job; a full `cargo kani` exceeded GitHub's 6-hour ceiling |
| S32 | The capability graph is observable, under an authority that observes and nothing else | `CAP_DEBUG` (roadmap 3.6) gates cross-task introspection and `SYS_CAP_ENUMERATE`, which reports one cspace slot's type, rights, serial, badge and generation. Minted **READ-only** in the root cnode, so no delegation can widen it, rights only ever narrow, so a primordial that never held WRITE cannot produce a descendant that does. It replaces the `CAP_AUDIT` init used to hand the shell for `ps`, which **also** rotates the audit chain's keys and reads the log: a bundling mistake rather than an ambient one, and invisible to an ambient-authority sweep because the gate was real: it just named far more authority than the caller needed. **Since 2026-08-24 cross-task introspection requires `CAP_DEBUG` specifically**: `SYS_GET_TASK_INFO` accepted `CAP_USER` or `CAP_AUDIT` as well until then, so "do you administer users" and "do you hold the audit log's keys" were both answers to "may I see the process list". Every holder that legitimately observes now holds the capability for observing: `proctest` keeps its `CAP_AUDIT` only because its *delegation* test needs a capability with an observable effect to hand a child. `object` is deliberately not reported: `serial` and `badge` are the graph's nodes and edges, so derivation is fully visible without naming what each capability points at (the same reasoning that suppresses `cr3` and another task's `eip`, finding I-4) | `make smoke-session` runs `capview` and requires the shell's own `debug` capability with `r-----` rights, not merely that something printed, since "no capabilities anywhere" is what a broken readout looks like. `make smoke-captest` requires refusal, four checks of its own; for a task holding no `CAP_DEBUG`, **including for its own cspace**. Falsified by `smoke-captest-capenum-control` (`CAP_ENUMERATE_UNGATED=1`), which removes the declared capability so the central gate admits everyone, and by `smoke-proc-taskinfo-control` (`TASKINFO_WIDE_AUTHORITY=1`), under which `grantee` (holding a granted `CAP_AUDIT` and no `CAP_DEBUG`) reads another task's info again |
| S33 | The security core's `unsafe` FFI code is free of the undefined behaviour an interpreter can see | `cargo miri test` over `rust/src`, 77 tests, every module that has tests except four crypto ones excused with a reason in `.github/miri-scope.yml`. Miri checks out-of-bounds access, invalid pointer use, and the Stacked Borrows aliasing rules that raw-pointer FFI is easiest to break, which is what this crate is: 80 `unsafe` blocks across `capability.rs`, `lib.rs`, `rng.rs` and `memory.rs`, each at the boundary where the C kernel hands in a pointer. **The first run found UB in three places, all of them in the TESTS**, a test would take `x.as_mut_ptr()`, store it, then reach the same array a second way, which retags and invalidates the first pointer. The C kernel holds one pointer and passes it twice, so the harness was modelling its own caller wrongly; the library was not at fault, which was established by **ablation** rather than assumed | `miri`, a required check, with no `continue-on-error`. `tools/check_miri_scope.py` fails the build on a test module that is neither run nor excused, and the job derives its `--skip` flags from that manifest so the scope and the command cannot drift. Falsified three ways: a rotted skip entry, an excuse of under eight words, and a new module (which defaults to RUN; the safe direction) |
| S34 | A clock is available to ring 3 without restoring the timer `CR4.TSD` denies | `SYS_CLOCK_GETTIME` reports monotonic time since boot from the **PIT tick counter**, not the TSC, so `nsec` is always a multiple of one tick (10 ms at `PIT_TICK_HZ`). `CR4.TSD` exists to remove "the cycle-accurate timer that cache/covert-channel attacks between mutually distrusting ring-3 tasks lean on" (`src/kernel/crypto.c`), and a nanosecond clock behind a syscall would hand it straight back. **This is not a claim of side-channel safety**, the TSD comment already says the mitigation is partial, and a counting loop still builds a finer timer; it is a refusal to make it easy. Ambient by design: a coarse count of time since boot is not authority over an object, and gating it would push callers toward a worse clock of their own. Every clock id but `HORUS_CLOCK_MONOTONIC` is refused rather than approximated: there is no RTC here, and answering `CLOCK_REALTIME` with uptime would be a number shaped like a date with nothing behind it. Monotonic **by construction**: the source is a counter the timer interrupt only increments, widened to 64 bits because a u32 at 100 Hz wraps in ~497 days | `make smoke-captest`: 7 checks; the resolution is a whole tick, `nsec` is in range, `reserved` is zeroed, a second read never goes backwards, and three refused clock ids. Falsified by `smoke-captest-clock-control` (`CLOCK_TSC_RESOLUTION=1`), whose defect is that it is **better**: real microseconds off the TSC, more accurate and more useful, undoing TSD |
| S35 | A mapping call that reports failure installs no mapping | `SYS_MAP_REGION(first_slot, count, vaddr, rights)` maps a run of `count` `CAP_FRAME`s at consecutive pages, and is **all-or-nothing**: if any page of the run cannot be mapped, every page the call already mapped is withdrawn before it returns, so a caller holding an error holds the address space it started with. A PTE is authority, so prefix semantics here would be a fail-**open**: the caller is told it got no region and holds part of one. The policy is deliberately the **opposite** of `SYS_RETYPE`'s, which stops at the first failure and returns a count: retype's partial result is complete information (n objects, each named by a capability at a slot the caller computed), while a partial map is a hole in a range whose purpose is to be addressed as a range, discovered later as a fault with nothing left to say which call was responsible. The unwind withdraws **only the pages this call installed**, never the pre-existing mapping that caused the refusal, which would otherwise let ring 3 destroy any mapping it dislikes by asking to map a region across it. Per-page validation is shared with `SYS_MAP_FRAME` in one function so the two cannot drift apart | `make smoke-frame` (58 parent checks). Falsified in **both** directions, one arm per rule: `FRAME_REGION_NO_ROLLBACK=1` (`make smoke-frame-region-control`) drops the unwind and the two pages mapped before the failure stay mapped: `FRAMETEST: FAIL region-rollback-page0`; `FRAME_REGION_ROLLBACK_WIDE=1` (`make smoke-frame-region-wide-control`) unwinds the whole *requested* range and eats the mapping that refused it: `FRAMETEST: FAIL region-rollback-ate-blocker`. Each arm fails **only** its own checks, and `smoke-frame` goes red under both |
| S36 | A frame capability names a run of pages, and every page of it is real | A `KOBJ_FRAME` carries a **length**: `SYS_RETYPE(untyped, KOBJ_FRAME, count, dest, pages)` carves `pages` contiguous pages as one object, `CAP_FRAME` names it, and `SYS_MAP_FRAME` / `SYS_UNMAP_FRAME` install and withdraw the **whole run** (roadmap 2.1's region object). Three properties follow and are each enforced: the pages are **distinct** physical pages, not `pages` aliases of the first: an aliased run maps, is writable, carries the right bits, and silently stores one page's worth, so nothing reports it; the **span** is bounded including its last byte, since an address legal for a one-page frame can put a four-page run past the user half (a second check, `user_pte_slot` refuses `pml4[256..511]` independently, which turns a part-way failure into one refusal before anything is installed); and every page is **pinned and scrubbed**, because a run pinned only at its head puts page 1 on the free page stack when the task dies, and a run scrubbed only at its head leaves the rest of a buffer readable by whoever the arena hands those bytes to next. The length is **refused, not ignored**, on a class that has none. Mapping a run is all-or-nothing by **S35**'s unwind, which is shared, so the same code, and the same arm, cover a failure inside one frame and a failure across a run of slots | `make smoke-frame` (58 parent checks). Falsified by `FRAME_PAGES_SAME_PHYS=1` (`make smoke-frame-pages-control`), which advances the virtual cursor and not the physical one: `FRAMETEST: FAIL sized-pages-distinct`, 3 boots in 3, and `smoke-frame` goes red under it. The arm also reddens the unmap checks, which is the defect showing rather than the test leaking: `unmap_run` expects page k to hold `base + k`, and under aliasing it does not |
| S37 | A frame's size is readable by a holder of its capability, and by nobody else | `SYS_FRAME_PAGES(frame_slot)` returns how many contiguous pages the `CAP_FRAME` at that slot names. Authority is **the capability the caller names** (a cspace slot resolved through `cap_lookup` and **type-tested**) never a frame index the caller supplies. An index argument would be finding **[C-1]**'s shape (authority read from somewhere other than the capability naming the resource) and an **object-existence oracle** besides: a task holding nothing could walk indices and learn which frames are live, and how large, across every task in the system. No rights floor (`cap_lookup(slot, 0)`, like `SYS_UNMAP_FRAME`): the size is not the contents, and requiring `READ` would refuse a `WRITE`-only sharer the ability to learn how much it may write. It is **not** a syscall that reads a capability; it reports the object's extent and nothing about rights, lineage or badge, and it discloses nothing `SYS_MAP_FRAME` does not already disclose to the same holder, which could learn the same number by trial-mapping forward | `make smoke-frame` (55 parent checks + 9 peer checks). Falsified by `FRAME_INFO_BY_INDEX=1` (`make smoke-frame-info-control`), which reads the argument as an index: `framepeer`, holding exactly one delegated `CAP_FRAME`, gets answers for slots 1 and 2, empty in that task, live frame indices in the kernel. `FRAMETEST: FAIL peer-frame-pages-not-an-index`, 3 boots in 3, and `smoke-frame` goes red under it |
| S38 | A page belonging to a kernel object is never copied out from under it | `cow_break_pte` refuses any physical page inside the untyped arena (`phys_in_untyped_arena`), returning a distinct `-4` that the pager propagates and `page_fault_handler` treats as unhandled; the task dies, fail closed. This is roadmap 2.1's *"what does a COW break mean for a capability two tasks hold"*, answered: it does not happen, because it would be **two** holes at once. **Resource authority**, the shared branch calls `alloc_user_physical_page()`, the *anonymous* pool, so a frame holder would obtain a private writable page that no untyped region ever paid for, which is ambient resource and contradicts roadmap 0.3's premise that creating a memory-backed object exercises authority the capability graph describes. And **identity**, the PTE would be repointed at a page no capability names, so the mapping silently stops being the object and the frame's `1 + mappings` pin arithmetic stops describing reality, which is what `destroy_dyn_frame` reads to decide a run is collectable. A task wanting a private copy retypes its own frame from its own untyped and copies the bytes: explicit, budgeted, visible in the graph. The guard covers the **whole arena**, not just frames, a cnode or an endpoint has even less business being copied out from under its object | `make smoke-nzcow`. Falsified by `COW_ARENA_UNGUARDED=1` (`make smoke-nzcow-arena-control`): `NZCOW_SELFTEST: FAIL arena-cow-broken`, 3 boots in 3, and `smoke-nzcow` goes red under it. The case is driven with a **real** `KOBJ_FRAME` at refcount 2, so the arm takes the *shared* branch that allocates: at refcount 1 an unguarded break merely upgrades the PTE in place, which is the wrong half of the defect to measure. **Nothing in the tree reaches this path today**, and the guard exists because the two things preventing it are circumstances rather than properties: `user_map_frame_page` never sets `PAGE_COW`, and `rust_validate_page_fault` admits only image, heap and stack. Roadmap 2.3's `fork` is what makes it reachable, and a frame mapped inside the heap window already passes the region gate |
| S39 | A forked child's memory is a copy of its parent's, not a share | `SYS_FORK` builds the child's address space with `clone_user_aspace`, which points both trees at the same physical frames and clears `PAGE_WRITE` / sets `PAGE_COW` **on both sides**. The first write from either side breaks the sharing through `cow_break_pte`, so each observes the bytes that existed at the fork and neither observes the other's later writes. Downgrading the **parent's** leaf is the load-bearing half: leave it writable and the result is not copy-on-write but a shared writable page with extra steps, and the two tasks are one process with two schedulable contexts: including one stack. Only present **user** leaves are cloned: the LAPIC and TPM windows are identity-mapped at low addresses and therefore sit in the *user half* of the PML4 while being supervisor-only, so cloning them would COW-mark an MMIO register file and fault the kernel in interrupt context; the child re-establishes its own through the same `ensure_*` calls. A huge user page is **refused** rather than split, because nothing builds one today and a splitting path nothing can test is worse than a refusal. Rollback needs no unwinding of the parent: freeing the child's tree returns every shared frame's refcount to 1, and the parent's next write takes `cow_break_pte`'s sole-owner branch and keeps the frame | `make smoke-fork`. Falsified in both directions by `FORK_SHARE_WRITABLE=1` (`make smoke-fork-share-control`), which clones the leaves unmarked: the child sees the parent's post-fork write (`FORKTEST: FAIL child-saw-parent-write`) and the parent reads back the child's (`FORKTEST: FAIL parent-clobbered`), and `smoke-fork` goes red under it. The witness itself is falsified too, a failing child **dies** rather than exits, and the parent reads that back with `SYS_TASK_EXIT_INFO`, because the first version printed a `FAIL` and then a `PASS` under this very arm. Re-asserted one layer on by `make smoke-forkexec`, which is the only path in the tree that **frees** a cloned address space while its parent is still running: `task_teardown` does not free the space at all, so a forked child that merely exits never exercises the reference the clone took: an exec does, through `create_user_pagedir`'s reclaim, and a reference dropped once too often puts a live page of the parent's on the free page stack. `FORK_SHARE_WRITABLE=1` falsifies that arm too (`FORKEXECTEST: FAIL parent-clobbered-by-exec`, 3 boots in 3) |
| S40 | A fork never shares a kernel object's page | A task holding a mapped `CAP_FRAME` is **refused** the fork outright: `clone_user_aspace` rejects any leaf whose frame lies in the untyped arena, before the child exists. **S38** already refuses the *break* of such a page, and this is the same policy one layer up: the difference is that S38 fires at fault time, on whichever side writes first, by which point two tasks exist and one of them dies at an unpredictable later instruction on a page it was entitled to write before the fork. The tempting alternative is to clone the frame *writable-shared*, on the grounds that a frame is shared memory: that is worse, because the child would hold a live mapping of a kernel object with **no capability naming it**, so revoking the parent's `CAP_FRAME` would sweep the parent's PTE and leave the child's behind. Authority here is the capability, and a fork that copies mappings but not the capability graph must not manufacture one without the other. Sharing a frame becomes expressible when fork learns to duplicate a cspace, deliberately a separate change (`docs/LIMITATIONS.md` §2.11) | `make smoke-fork`, which checks **both** directions: the fork is refused with a frame mapped, and the identical call succeeds once it is unmapped, so the refusal is about the frame rather than about fork being broken, which a refusal-only gate cannot distinguish. Falsified by `FORK_ARENA_UNCHECKED=1` (`make smoke-fork-arena-control`): `FORKTEST: FAIL forked-with-frame-mapped`, 3 boots in 3, and `smoke-fork` goes red under it |
| S41 | A forked child's capabilities are derived from its parent's, never duplicates of them | `SYS_FORK` gives the child a copy of every capability the parent holds, in the same slot, through `cap_clone_cspace`, and each copy is made by **`rust_cap_grant_into`**, the same derivation `SYS_CAP_GRANT` uses, so a forked capability and a delegated one are the same object by construction rather than by two implementations agreeing. Each copy therefore carries its **own fresh serial** and a `badge` naming the parent capability's serial, which is the edge `revoke_subtree` walks. Two consequences, and they are the property: the child's authority is a **subtree** of the parent's, so fork adds no new **root** to the capability graph and every revocation that would have swept the parent's capability sweeps the child's with it; and no two capabilities share an identity, so a revocation aimed at one cannot null another in a different task's cspace. Rights are `CAP_RIGHT_ALL & src->rights`: exactly the parent's, never more. Rights are deliberately **not** narrowed the way `SYS_SPAWN` narrows the console capability, because spawn hands authority to a *different program* while fork's child is the same program at the same instruction; a silently reduced copy would break `if (fork() == 0) serve();` with nothing to report it, and the program would have the parent grant the rights back, achieving nothing but a less legible graph. **Not copied:** slots 0–3 and slot 4, which are the child's own identity: the parent's slot 0 names the *parent*, so copying it would mint a `CAP_TCB` over the parent that the parent never held in a form it could delegate, and slot 4 is the private reply endpoint whose entire value is that nobody else has it (finding **C-1**), and `CAP_REPLY` by type, since a one-shot reply held by two tasks is a reply-forgery primitive. A revoked or lookup-invalid source is skipped, and hitting `MAX_CAPS_PER_TASK` fails the whole fork rather than handing over a slot-order-dependent prefix of the parent's authority | `make smoke-fork`, which reads the derivation graph directly with `SYS_CAP_ENUMERATE`: `serial` and `badge` are the graph's nodes and edges, so the invariant is checked **structurally** and needs no rendezvous between parent and child, which matters because a forked child shares nothing it could rendezvous through. Falsified by the two ways to get it wrong, one arm per rule: `FORK_CSPACE_FLAT_COPY=1` (`make smoke-fork-cspace-flat-control`) copies verbatim so two capabilities share a serial: `FORKTEST: FAIL child-cap-shares-serial`; `FORK_CSPACE_ORPHAN_COPY=1` (`make smoke-fork-cspace-orphan-control`) gives a fresh serial but no parent edge, `FORKTEST: FAIL child-cap-not-derived` **and** `FORKTEST: FAIL child-cap-survived-revoke`, the structural claim and its end-to-end consequence going red together. Both 3 boots in 3; `smoke-fork` goes red under each |
| S42 | An exec replaces the image, not the authority | `SYS_EXEC_NAMED` / `SYS_EXEC_IMAGE` rebuild the address space and **do not touch the cspace**: the task that comes back holds the same capabilities it held going in, with the same `serial`, the same `badge`, and therefore the same position in the derivation graph. Two things follow, and the second is why this is a security property rather than POSIX nostalgia. A revocation aimed at whatever those capabilities were derived from still reaches them, so **a task cannot launder delegated authority into a root of its own by execing**. And combined with **S41**, `fork(); exec();` (the only sequence a shell ever performs) produces a task whose authority is still a *subtree* of its parent's, three generations deep: the parent's capability, the child's forked copy, and whatever the child minted from it before the exec. The property is written as the **absence** of a step, which is the hardest kind to hold: there is no line to point at, so nothing goes stale visibly and no reviewer is prompted to ask. The two ways an exec is tempted to touch a cspace are the two control arms, discarding it ("a fresh start for a fresh image") and re-minting it ("the new image should own what it holds"), and the second is invisible to every functional check, because the authority is identical and only the lineage is gone | `make smoke-forkexec`, which reads the child's cspace out of the derivation graph with `SYS_CAP_ENUMERATE` after the exec and then **revokes**, so the structural claim and its end-to-end consequence are both checked. Falsified in both directions, one arm per rule: `EXEC_RESET_CSPACE=1` (`make smoke-forkexec-reset-control`) → `FORKEXECTEST: FAIL exec-dropped-inherited-cap`; `EXEC_ROOT_CSPACE=1` (`make smoke-forkexec-root-control`) → `FORKEXECTEST: FAIL exec-recreated-inherited-cap`, `exec-orphaned-inherited-cap` **and** `child-cap-survived-revoke-after-exec`. Each arm fails only its own rule's checks; both 3 boots in 3, and `smoke-forkexec` goes red under each |
| S43 | Hardware authority names a device, and reaches only that device | A `CAP_IO_DEVICE`'s `object` is an index into the kernel's I/O-device table (`src/kernel/pci.c`), built once at boot from a PCI bus scan plus one non-enumerable **platform** entry for the legacy console hardware. Each entry declares exactly the three things the device syscalls hand out (physical frames, I/O port ranges, and interrupt lines) and `SYS_MAP_PHYS`, `SYS_IOPORT_GRANT` and `SYS_IRQ_REGISTER` each take a **cspace slot** as their first argument, resolve it through `iodev_from_slot`, and check the resource asked for against what **that** device declares. Their fixed slot-10 dispatch-table entries are gone: the per-slot lookup *is* the gate, exactly as it is for IPC, and leaving the entry would have re-admitted the old behaviour underneath the new check. Until 2026-08-28 this was **finding [C-1]'s shape one layer down**: the capability's object was never read and the resources came from constants (a compiled-in VGA allowlist, one compiled-in console port set prefilled into the TSS bitmap at boot, a hardcoded pair of IRQ numbers), so holding *any* device capability was holding the console, and a second driver could not be given the hardware it needs without the hardware it does not. Index 0 is reserved as "no device" so that the two things which default to zero, a task slot's `io_device` and a capability's `object`, fail **closed** rather than resolving to the console. Port authority follows the capability into the TSS: the bitmap is loaded from the granted device's ranges on every switch in, so a grant is to one device and a regrant replaces it rather than accumulating. PCI configuration space (`0xCF8`/`0xCFC`) is declared by no device and reachable by nobody, because a driver that could write it could move any device's BARs and make every check above decorative | `make smoke-devcap`, whose probe holds **two** device capabilities; the platform device and the machine's NIC, and checks the matrix in both directions on one boot: each reaches its own device's frame, port and IRQ, and is refused the other's. Both directions are required; the negatives alone are satisfied by a kernel that refuses everything. Falsified one arm per rule: `IO_DEVICE_OBJECT_UNCHECKED=1` (`make smoke-devcap-object-control`) → `DEVCAPTEST: FAIL nic-cap-mapped-vga`; `IO_DEVICE_PORTS_GLOBAL=1` (`make smoke-devcap-ports-control`) → `FAIL nic-cap-got-console-ports`; `IO_DEVICE_IRQ_UNCHECKED=1` (`make smoke-devcap-irq-control`) → `FAIL nic-cap-took-platform-irq`. Each arm breaks exactly one marker, which is what shows the three checks are independent; and `smoke-devcap` goes red under each |
| S44 | Turning a device into a bus master is authority, held by a capability, and it is the one authority this machine cannot bound | `SYS_DEVICE_ENABLE` sets the three PCI decode bits (I/O, memory, bus master) of the device the caller's `CAP_IO_DEVICE` names, and **nothing else in configuration space**: the offset is fixed in `iodev_set_decode`, the value is masked to three bits, and the device is the one the capability named rather than one passed by address. That narrowness is load-bearing; the BARs live in the same 256 bytes, and a driver that could write them could move its own BAR onto another device's registers and make **S43**'s frame check a lie. `SYS_DMA_ADDR` reports the address at which a named device reaches a named frame, and requires **both** capabilities: the answer is a physical address, disclosed nowhere else in this ABI, and gating on the frame alone would hand the kernel's memory layout to every task that ever retyped a page, for no purpose any of them could act on. Requiring the device makes the disclosure add **nothing**: a holder of a bus-mastering device capability can already read and write all of physical memory through its device. **What this claim does not say, and S45 now does:** turning bus mastering on decides that a device may touch memory, never *which* memory. Until VT-d landed (2026-08-28) that meant all of it; since S45 a device reaches only what its driver mapped. This claim remains the gate on **who** may make a device a bus master and **for which device**. S45 is the gate on where it then goes. `docs/LIMITATIONS.md` §2.12 | `make smoke-net`: `netd`, an Intel e1000 driver whose whole authority is one device capability plus one untyped region, completes a full DMA round trip and receives the reply, end to end on the wire rather than a register read. It drives e1000 rather than virtio deliberately: a paravirtual device accesses guest memory directly and is not on the far side of the IOMMU at all, so it cannot witness the neighbouring claim. Falsified one arm per rule: `NET_NO_DECODE=1` (`make smoke-net-decode-control`) → `NETTEST: FAIL mac-not-valid`, the device stops answering its own BAR; `DMA_ADDR_FRAME_ONLY=1` (`make smoke-frame-dma-control`) → `FRAMETEST: FAIL dma-addr-without-device-cap`, the two-capability rule dropped. Each 3 boots in 3, and each turns its base gate red. **Not** falsified for bus mastering itself: measured 2026-08-28, QEMU does not enforce the bit for virtio-net, so `NET_NO_BUSMASTER` reproduces nothing here and is kept ungated with that reason written down |
| S45 | A device reaches only the memory its driver mapped for it | Intel VT-d DMA remapping (`src/kernel/iommu.c`), brought up before any ring-3 task exists. Every device has an **address space of its own**, exactly as a task does, and **every one of them starts EMPTY**, not an identity map of the pool, not "everything below 4 GiB". A device whose driver has mapped nothing reaches nothing and every address it emits faults. That default is the property: an identity map would have been far easier and would make the whole mechanism decorative, since the device would reach all of memory again through a translation that always says yes. `SYS_DMA_ADDR` is what installs a mapping, so a driver's DMA reach is exactly the frames it holds a `CAP_FRAME` for and has asked to map, and the mapping carries **that capability's own write right**, so a READ-only frame yields a READ-only device mapping and a compromised device cannot scribble on a page its driver may only read (**S27**'s rights floor, extended to hardware). Every device gets its **own domain**: sharing one would make a mapping installed for one device reachable by another, which is **S43**'s defect one layer further down and invisible because both devices would still work. This is what **S44** could not say: that claim gates *who* may turn a device into a bus master, and could not bound where the device then went, because nothing in software can. `docs/LIMITATIONS.md` §2.12 | `make smoke-net`: `netd`, an e1000 driver whose whole authority is one device capability plus one untyped region, completes a full DMA round trip: the device **reads** its descriptor ring, **reads** the packet buffer, and **writes** the completion status back, every address of which was mapped by `SYS_DMA_ADDR` and by nothing else. Falsified by `NET_IOMMU_NO_MAP=1` (`make smoke-net-iommu-control`): the same driver, the same device, every capability check still run and every address still correct, only the *mappings* withheld, and the device cannot read its own ring. `NETTEST: FAIL dma-never-completed`, 3 boots in 3, and `smoke-net` goes red under it. The gate boots under the `SMOKE_IOMMU` harness mode with an **e1000**, not virtio: a paravirtual device accesses guest memory directly and is not on the far side of the IOMMU at all, so it cannot witness this, measured, and recorded in `userspace/netd.c` |
| S46 | An unserviced device interrupt cannot livelock the machine, and re-enabling one is authority | A legacy PCI interrupt is **level-triggered**: the device holds the line asserted until its driver services it. So when a registered line fires, the kernel **masks it before acknowledging it** (`src/kernel/idt.c`), in the I/O APIC's redirection table where one is present, or the 8259's mask register where it is not, through one entry point so the two cannot disagree about which lines are live, delivers the notification, and leaves it masked until the driver calls `SYS_IRQ_ACK`. Without that, an EOI is followed by immediate re-delivery, forever, ring 3 never runs, the driver never clears the device, and the machine stops making progress with no software able to intervene. That is a denial of the **whole machine** reachable from a capability meant to name one device, so masking is what keeps a broken driver's blast radius to its own hardware. `SYS_IRQ_ACK` is gated exactly as `SYS_IRQ_REGISTER` is: a `CAP_IO_DEVICE` naming a device that **declares** the line (**S43**), because unmasking decides that a device may interrupt this machine again; and holding the right capability is still not sufficient, since `irq_notify_ack` refuses unless the registration belongs to the **calling task**, so two holders of one device capability cannot acknowledge each other's interrupts. A dead driver takes its line down with it: `irq_notify_clear_task` masks on teardown, because a level-triggered line left asserted with nobody to service it is the same storm arriving by another route. IRQ 0 is exempt, masking the preemption tick would stop the scheduler | `make smoke-net`: `netd` registers its NIC's own declared line, enables the device's transmit-completion interrupt, is woken by a **real hardware interrupt**, services the device and acknowledges it (`NETTEST: IRQ PASS`). Falsified **directly**, on the routing the ship build uses, by `IRQ_NO_MASK_ON_FIRE=1` (`make smoke-net-mask-control`): the device's cause is cleared and a second transmit re-raises the line, and while the line is masked `SYS_POLL_NOTIFY` must report `IPC_AGAIN` on every attempt, a notification arriving there is `NETTEST: FAIL irq-while-masked`, 3 boots in 3, and `smoke-net` goes red under it. That test is only possible because a caller can now observe that a notification did **not** arrive; before `SYS_POLL_NOTIFY` a blocking wait could only return (the property is broken) or hang (the property held and the test times out indistinguishably from a crash), so the property had to be witnessed through its catastrophic consequence instead. The livelock arm survives as `make smoke-net-irq-storm-control` on the **8259 path** (`IRQ_FORCE_PIC=1`) (measured, QEMU storms on the PIC and not on the I/O APIC) which keeps the fallback controller exercised too. The authority half is witnessed in `smoke-captest`, which holds no device capability at all: `IRQ_ACK_UNGATED=1` (`make smoke-captest-irq-ack-control`) → `CAPTEST: FAIL irq-ack-without-cap-io-device` |
| S47 | A device's interrupt vector is chosen by the kernel, never by its driver | An MSI is not a wire: it is a memory **write** the device performs, to an address the LAPIC decodes, carrying a data word whose low byte is the interrupt **vector**. So with MSI, *which interrupt does this device raise* stops being a fact about the board and becomes a value in a register: a much sharper question than INTx posed, where the worst a driver could do was subscribe to a line its device does not own (**S43** already refuses that). A device that could be pointed at any vector could raise the timer's, another driver's, or an exception gate: #GP is vector 13, and a device raising 13 at will could make any task appear to fault anywhere. **`SYS_MSI_REGISTER` therefore takes no vector argument**, it takes a `CAP_IO_DEVICE` naming the device and a `CAP_NOTIFICATION` to be woken on, and nothing else. The kernel allocates a vector from a range it owns (48–63, disjoint from the legacy 32–47 block so it cannot collide with an I/O APIC pin, and below 64 so it cannot collide with the LAPIC timer), programs the capability registers itself, and records where the resulting interrupt is delivered. Ring 3 has nowhere to express a preference, and `SYS_DEVICE_INFO` reports *whether* a device has MSI and never *where* the capability is, because the offset names the register carrying the vector. Every write to it lives in `src/kernel/pci.c`, the one file that touches configuration space at all: exporting a general config-space write so another file could program MSI would hand every future caller the ability to choose a vector, or to reprogram a BAR (**S43**). This is only enforceable *because* configuration space is unreachable from ring 3 and `SYS_DEVICE_ENABLE` reaches exactly three decode bits (**S44**); MSI is what makes that strictness load-bearing rather than tidy. Unlike **S46** there is no masking and no acknowledgement: an MSI is edge by construction, so no line stays asserted and there is no livelock to prevent | `make smoke-net`: `netd` drives an 82574L, whose MSI capability the kernel finds by walking configuration space at boot, and is woken by a **message-signalled interrupt on a vector it never named**, then completes an ARP exchange. Falsified by `MSI_VECTOR_FROM_USER=1` (`make smoke-net-msi-vector-control`), which honours a caller-supplied vector and has `netd` ask for **13**: the NIC raises #GP at the machine, the kernel processes a general-protection fault that never happened against whatever the message interrupted, and the run dies: asserted as a **fault**, 3 boots in 3, because that is what the defect *is*. The assertion names the task the fault is attributed to rather than the flavour of fault, since a misinterpreted exception frame goes wrong in whichever way it goes wrong first |
| S48 | A driver cannot map its own device's MSI-X vector table | An MSI-X table entry carries a message address and a data word whose low byte is the interrupt **vector**: the same field **S47** keeps out of a driver's hands. But unlike MSI's, it does **not** live in configuration space: it lives in a **BAR**, in ordinary device memory a driver maps page by page with `SYS_MAP_PHYS`. S47's mechanism therefore does not transfer, because *"the kernel programs it"* is no answer when the driver can program it too. So the kernel resolves the table's physical extent at boot (from the capability's BAR index and offset, against the sized BAR) and `iodev_allows_mmio` refuses **any page overlapping it**, to a driver holding a perfectly good `CAP_IO_DEVICE` for that device, on a page inside a BAR the device declares. Authority over a device is not authority over the register that decides which interrupt the machine takes. The refusal is at **page** granularity and fails closed: a device that ignores the PCI spec's advice to give its table a private 4 KiB region loses its neighbouring registers along with it, because denying a driver registers costs a feature while allowing the write costs the machine. It covers READ as well as WRITE, so the rule is about the page rather than about writes: otherwise the next "harmless" read path has a precedent. The kernel keeps its own supervisor-only mapping of the same physical page, established at boot: that asymmetry *is* the property. **Scope, stated plainly: the kernel does not yet ENABLE MSI-X**, so this is defence in depth rather than load-bearing today; the table is inert while MSI-X Enable stays clear, and that bit is in configuration space which ring 3 cannot reach. It is in place first deliberately, so that enabling MSI-X later cannot forget it; `docs/LIMITATIONS.md` §2.15 records why enabling is deferred | `make smoke-net`: `netd` is told where its device's table page is (`SYS_DEVICE_INFO` reports the page, since knowing the address buys nothing against a refusal) and is refused it both writable and read-only: `NETTEST: MSIX PASS`. Falsified by `MSIX_TABLE_MAPPABLE=1` (`make smoke-net-msix-table-control`), which drops the refusal: the driver maps its own vector table with no syscall, no capability check and nothing to gate. `NETTEST: FAIL msix-table-mapped`, 3 boots in 3, and `smoke-net` goes red under it |
| S49 | Shared library text is executed by many tasks and writable by none | A shared library is loaded **once** into frames at boot (`src/kernel/shlib.c`) and every task maps it through a `CAP_FRAME` carrying READ and EXEC and **never** WRITE. The enforcement is **S27**'s rights floor rather than the loader's intent: the primordial capability over those frames never held WRITE, and rights only ever narrow on delegation, so no descendant of it can carry WRITE and there is no path by which any task obtains a writable mapping of code another task is running. **Why this needs saying at all:** sharing a library is a memory optimisation, but sharing it *writably* is a code-injection primitive between every task that maps it (one task patches a function and another executes the patch) which is strictly **worse** than the per-program static copies it would replace, because those at least isolated the damage. The library's text is relocated once against a fixed base and mapped at that same address everywhere, because text needing per-task relocation would not be shared text at all; the cost is that the library gets no ASLR, which is recorded in `docs/LIMITATIONS.md` §2.16 rather than glossed. Loading refuses anything but `R_X86_64_RELATIVE`, a half-relocated library is one whose calls go somewhere nobody chose | `make smoke-shlib`: two tasks are endowed with capabilities over the **same** frames, map them independently, execute the library, and each fail to obtain a writable mapping of it, asking for WRITE, and for WRITE\|EXEC, both refused. Falsified by `SHLIB_TEXT_WRITABLE=1` (`make smoke-shlib-writable-control`), which is the attack rather than a stand-in: the capability carries WRITE, `shlibtest` maps the library a second time writable and overwrites a function through that alias while executing through its read+exec one, and the **peer**: which asked for nothing but read+exec, runs the patch. `SHLIBTEST: FAIL peer-saw-patched-code`, 3 boots in 3, and `smoke-shlib` goes red under it. **Two tasks, not one:** a single task proving it cannot write its own page proves less than it looks, since the failure worth excluding is cross-task |
| S50 | A shared library's writable data is private to each task | A shared object's `PF_W` segments are **not** shared. `shlib_init` records which pages a writable `PT_LOAD` touches and keeps their frames as a **template** (the library's initial image, which no task ever maps) and `shlib_instantiate_data` carves a fresh frame per task and copies that template into it. Text is endowed from a primordial carrying READ\|EXEC and never WRITE (**S49**); data from a separate primordial carrying READ\|WRITE and never EXEC, so a page a task can write is a page it cannot jump into. **Note where the isolation lives:** not in the rights, which are identical for every task, but in the *object* each capability names being a different frame. Rights say what may be done; the object says to what. **Why a libc needs this.** Measured 2026-08-29 going into the newlib migration: of the 59 newlib symbols the shipped coreutils reference, exactly three are writable, `_impure_ptr` (errno, the stdio buffers, the atexit list, the rand state), `optarg` and `optind`. Shared, one task reads and writes another's errno and stdio buffers and can corrupt an allocator another task is mid-call in. That is the same argument **S49** makes about text arriving one segment further on, and it is why the split is enforced by the loader rather than by a linker script: `userspace/shlib.ld` page-aligns the boundary so the ambiguous case does not arise, but a linker script is not an enforcement mechanism and the page-marking loop does not depend on it holding. Rounding is outward: a page shared between a read-only and a writable segment is counted writable, because the imprecision must fall on the side of *this task gets its own copy* | `make smoke-shlib`: two tasks map the same library, each reads the library's initialiser out of its own data copy, and one writes a sentinel the other must never see. The witness is the **peer's**, deliberately: a task cannot tell a private copy from a shared one by looking at its own writes, so only the task that wrote nothing can witness privacy. Falsified in **two separable directions**. `SHLIB_DATA_SHARED=1` (`make smoke-shlib-data-shared-control`) endows every task with the same data frame: `SHLIBTEST: FAIL peer-saw-our-data`, 3 boots in 3: disclosure. `SHLIB_DATA_UNINITIALISED=1` (`make smoke-shlib-data-init-control`) gives a private frame but zero-fills it: `SHLIBTEST: FAIL data-not-initialised`, 3 boots in 3, loss. `smoke-shlib` goes red under each. The expected value is read from the library's own **text** (`shlib_state_initial`) rather than written down in the test, so the comparison holds no second copy of a constant that could drift |
| S51 | The shared library's load address is drawn per boot, and is not ambient information | `shlib_init` picks the library's base from `aslr_random_offset` -- the same CSPRNG-seeded source the image loader uses, rejection-sampled over 2^30 page-aligned positions (30 bits, matching the image window) -- **before** the object is relocated, and relocations and the export table are resolved against it. Shared text must be at the same address in every address space, which is what makes it shared, so per-boot is the strongest randomisation this mechanism admits: one information leak reveals the library for every task rather than for one. That is weaker than per-process ASLR and far stronger than an address printed in the binary. **Why it lands before the thing it protects.** The base was compiled in, which was defensible for a three-page demo and stops being so the moment newlib moves onto this mechanism (roadmap 2.5): ~135 KiB of executable code at a known address in every task, and a *regression*, since a program's static libc today sits inside its own PIE image which the loader already randomises. **The base is answered only to a holder.** `SYS_SHLIB_INFO` requires a `CAP_FRAME` + READ naming one of the library's own **text** frames; a wrong type is refused, and so is a `CAP_FRAME` of the wrong *object* -- including the caller's own private copy of the library's writable page (S50), which it legitimately holds and which says nothing about holding the code. Ungated, an attacker with execution in any task could simply ask where the shared gadgets are, and the randomisation would protect nothing | `make smoke-shlib-aslr`: **two boots**, because the property is not observable from inside one -- a single run sees an address either way. Both boots report their base and the two must DIFFER; both must also reach `SHLIBTEST: PASS`, so a boot that died before printing one cannot read as success. 3 runs in 3. Falsified by `SHLIB_BASE_FIXED=1` (`make smoke-shlib-aslr-control`), the same script with the comparison inverted: the two bases must be IDENTICAL, 3 in 3, and the base gate goes red under the flag. The refusals are falsified separately and one arm per rule, because with a single arm the object rule would never have been shown to fire: `SHLIB_INFO_UNGATED=1` removes the capability test entirely (`SHLIBTEST: FAIL shlib-info-with-wrong-cap-type`, 3 in 3) and `SHLIB_INFO_TYPE_ONLY=1` keeps the type check and drops the object test, so the task's own data-page capability answers (`SHLIBTEST: FAIL shlib-info-with-data-frame`, 3 in 3). `smoke-shlib` goes red under each. **What two unequal draws do not prove:** they are consistent with a 1-bit source. The gate asserts that the base is not a constant; the entropy claim rests on `aslr_random_offset`, stated rather than implied |
| S52 | An unauthorised capability operation is refused by returning, never by halting the CPU | Every other row here is about which calls are refused. This one is about the refusal arriving. `cap_mint` and `cap_transfer` resolve the caller's SOURCE slot through `kcap_lookup`, which is now `cap_lookup` and nothing else (`src/kernel/capability.c`). Until 2026-08-29 it was `cap_lookup` followed by `kassert_cap`, an unconditional `for(;;){}` on NULL, executed while holding `cap_lock` with interrupts masked by `spin_lock`'s own `cli`. All three inputs that make `cap_lookup` return NULL are chosen by the caller (a slot past `CNODE_SIZE`, an empty slot, or a slot without `CAP_RIGHT_MINT`), `h_cap_mint` passes `rbx`/`rcx`/`rdx` through untouched, and `SYS_CAP_MINT`, `SYS_CAP_TRANSFER` and `SYS_CAP_MOVE` are `SC_NONE` entries whose comment delegates authority to the primitives. So one syscall from any unprivileged ring-3 task stopped the CPU inside a global critical section, and every other CPU that then wanted `cap_lock` stopped behind it. The `if (!src ...) return false` beneath the call was already the intended behaviour and was simply unreachable. **Why this is not the documented local-DoS exemption:** that exemption covers a task spending its own share (spinning, allocating, forcing a broad revoke). This ends the machine for every task, from ring 3, with no capability and no quota consumed. Recorded as its own property because a refusal that never returns is indistinguishable from a hang, and nothing else in this table asks for the return. The three syscalls involved were also the three that no tracked workload entered, which is `docs/LIMITATIONS.md` §1.8 doing exactly what it warns it does | `make smoke-captest` (182 checks), whose section 12 hands `cap_mint`, `cap_transfer` and `cap_move` an empty and an out-of-range source slot and requires each to come back, then mints from a capability it does hold so the four refusals cannot be satisfied by a call that refuses everything. Falsified by `CAP_LOOKUP_ASSERT_HANG=1` (`make smoke-captest-mint-hang-control`), which restores the halting assert: measured 2026-08-29, the guest reaches `CAPTEST: cap-derivation-probes` and never prints `CAPTEST: PASS`. The arm asserts a STALL rather than a marker, because a task wedged inside a syscall prints nothing, and `EXPECT_STALL` makes that two-sided: the probes must be reached and then not finish, so a boot that died earlier fails the arm instead of satisfying it. `make smoke-captest` goes red under the same flag, confirmed by running its own assertion against the arm's ISO. Reproduced on a uniprocessor boot, where the one CPU is the whole machine; the SMP amplification through `cap_lock` follows from the lock being global and is not separately measured |
| S53 | A device's translation of a page does not outlive the capability that authorised it | `SYS_DMA_ADDR` installs an IOMMU entry for the frame a driver names, and until 2026-08-29 nothing removed it. `frame_map_refcount` counts CPU mappings only, so a device mapping neither kept the frame alive nor was torn down when it died: `destroy_dyn_frame` scrubbed the run, released the pages to the arena, and left the translation installed. The arena then handed those bytes to a fresh object while a bus-mastering device could still read and write them. That is a device-side use-after-free, and it is the third capability-free path to a page in a function whose own comments reason carefully about the second. `destroy_dyn_frame` now calls `iommu_unmap_all` **before** the scrub, so there is no window in which the run is both reallocatable and reachable, and `task_teardown` resets the dying task's device domain, which is the case the frame path does not cover: a frame held by two tasks survives the driver's death, and the driver's device would otherwise keep its mapping of it. Destruction tears the mapping down rather than refusing to collect, which is the opposite of the policy for a live PTE one branch above, and deliberately so: a PTE's holder is a live task still reading the bytes, while a device mapping's authority is the capability being destroyed and its holder is hardware that cannot be asked to stop. `docs/LIMITATIONS.md` §2.12 | `make smoke-iommu-teardown`, an in-kernel witness that maps a freshly retyped frame into a real device's domain under a real VT-d unit, asserts the translation is present, destroys the frame through the shipping `destroy_dyn_frame`, and asserts it is gone. Falsified by `IOMMU_NO_FRAME_TEARDOWN=1` (`make smoke-iommu-teardown-control`): measured 2026-08-29, `IOMMUTEST: FAIL device-still-translates-destroyed-frame`. **What this witness does not show:** it reads the device's second-level table through a kernel-internal query, so it establishes that the entry is removed, not that the device observed the removal. The IOTLB invalidation that makes those the same thing is `iommu_flush`, which `iommu_unmap` already issues and which this test does not separately witness. A DMA-based witness was rejected rather than skipped: proving it with a packet means pointing a live device at a page the arena has already reallocated, which is committing the defect in order to observe the fix. The `task_teardown` half has **no arm**, deliberately and stated rather than implied: reproducing it needs a driver holding a device capability to die under `SMOKE_IOMMU` while a peer still holds the frame, and no workload in this tree does that yet |
| S54 | Every `unsafe` in the security core states what its caller must uphold | `CLAUDE.md` §7 has always required a `# Safety` clause on every `unsafe`, and until 2026-08-29 nothing enforced it: **30 of 49 production sites had none**, worst in the two modules the kernel trusts most, `rust/src/capability.rs` (10 of 14) and `rust/src/memory.rs` (6 of 6, the string appearing nowhere in the file). This matters more here than in ordinary Rust because every one of these functions is called from C, and **C cannot be made to uphold an obligation nobody wrote down**. Neither of these is visible from a signature: `rust_cap_revoke_global` needs *every* cspace in the system in its `spaces` array or a revoke silently misses a derived copy (**S3**), and `rust_page_ref_inc` needs `page_lock` held because it is a non-atomic read-modify-write on the refcount that decides when a page is freed. All 49 now say what the caller must uphold and, where it matters, what the function does **not** check: `rust_password_hash` records that a null pointer is rejected but a length overstating its buffer cannot be, and `rust_free_user_physical_page` that it does not verify the page was ever allocated, naming the refcount discipline in `paging.c` as what prevents a double free. This is a documentation property with a mechanical gate, not a proof of soundness | `unsafe-safety`, a required check (`tools/check_unsafe_safety.py`), which walks `rust/src/*.rs`, skips test modules, and requires a clause on the enclosing item of every `unsafe`. **What it does not establish, stated in the tool itself:** it proves an obligation is *written*, never that it is true, complete, or upheld by any caller, a clause reading "none" would satisfy it. It is a floor; `miri` and `kani-bounded` test the code beneath. Falsified four ways by `tools/test_check_unsafe_safety.sh`, run in the same job: an undocumented production `unsafe` is caught and named; the same inside a test module is ignored; an item cannot inherit its neighbour's clause; and an `unsafe { }` block is reported when its **enclosing** item loses its clause, which is where a block's obligation is discharged. The third arm exists because the harness **found that defect in the checker on its first run**, a fixed 30-line lookback let an undocumented item inherit the clause of the function above it, and correcting it surfaced a 30th undocumented site (`rust_hmac_sha256`) that the manual count had missed |
| S55 | A capability lookup resolves against the CALLER'S OWN cspace, or refuses | `cap_lookup` (`src/kernel/capability.c`) is the single resolver every capability gate in this kernel goes through, and until 2026-08-30 it ended in an unconditional fallback: `if (cspace && slot < cspace_sz) {...} else { root_cnode }`. Two distinct callers reached that `else`. A task with **no cspace** resolved every slot against the primordial root cnode -- `CAP_TCB` over task 0, `CAP_CONSOLE`, `CAP_KERNEL_LOG`, `CAP_USER`, `CAP_ENCRYPTED_STORAGE`. And a task asking for a slot **past the end of its own cspace** was handed `root_cnode[slot]`: the identical escalation reached by arithmetic rather than by a null pointer, needing no missing cspace at all, and documented nowhere. Both were unreachable, and both by CIRCUMSTANCE rather than by property -- the first rests on `create_task` halting rather than running a task whose cspace allocation failed, the second on it setting `cspace_size = CNODE_SIZE` for every task. Neither is a statement about `cap_lookup`. The rule that replaced them was already in the file: `caller_has_authority()` encodes `cur == 0 \|\| tasks[cur].cspace != NULL` for the *mutating* operations, precisely so the `cspace == root_cnode` rights exemption in `cap_mint`/`cap_transfer` provably means "kernel only". Task 0 is the boot/idle/reaper and legitimately has no cspace; every other task without one is refused, and an out-of-range slot is out of range. This is also what unblocks reclaiming a dead task's cspace, the stated blocker on the last of **[I-7]** | `make smoke-cap-lookup`, which MANUFACTURES both conditions -- a scratch task with its cspace nulled and one with a deliberately short cspace -- because a refusal test whose ungated path could not have succeeded witnesses nothing. It checks the third direction too, that task 0 still resolves: a `cap_lookup` returning NULL for everything satisfies both refusals while breaking every gate in the kernel. Falsified one arm per rule: `CAP_LOOKUP_ROOT_FALLBACK=1` (`smoke-cap-lookup-control`) restores the whole `else` and the cspace-less probe resolves a live `CAP_CONSOLE`; `CAP_LOOKUP_RANGE_FALLBACK=1` (`smoke-cap-lookup-range-control`) restores only the out-of-range half, which the first arm cannot reach because the witness returns at its first failure. Base gate red under each |
| S56 | A task's authority ends when the task does | `task_teardown` (`src/kernel/scheduler.c`) releases every device resource a task held -- its IRQ route, its MSI route, its IOMMU domain, its port grant, the console, its pipe ends -- and until 2026-08-30 left its CAPABILITIES in its cspace. They sat there from the task's death until its slot was next used, which may be never. Nothing could reach them, and that is the part worth stating: three separate readers each avoided a dead cspace by testing `state == 0` -- `mark_reachable` (untyped.c) when deciding which retyped objects are still named, `h_cap_enumerate` when reporting, `create_task` when overwriting. The property was held by three readers agreeing about a flag rather than by the data, and a fourth reader that forgot the flag would have found a full cspace, including the task's own `CAP_TCB`. `cap_release_cspace` empties it at teardown, BEFORE `kobj_gc`, so the object sweep sees a genuinely unnamed object rather than one it is skipping on the strength of that flag. **What is NOT reclaimed is the bytes**, and must not be: the arena is a monotonic bump allocator, which is what makes type-confusion-through-reuse structurally impossible, and `UNTYPED_KERNEL_BYTES` holds exactly `MAX_TASKS` cspaces, so a free-then-reallocate would exhaust the reserve on the first slot reuse and halt `create_task`. The bytes belong to the slot for the life of the boot; only the contents are reclaimed, which is what "reclaim" already means for every other object class in `untyped.c` | `make smoke-cspace-release`, which plants the primordial `CAP_CONSOLE` in a scratch task, tears it down through `task_teardown` (not by calling the release -- a witness that calls the function under test says nothing about whether anything invokes it), and requires EVERY slot of its cspace to be empty. It also requires the cspace POINTER to survive and the slot to be reusable, since a teardown that destroyed the cspace outright, or a `create_task` that could no longer install anything, would satisfy the emptiness check while breaking task creation. Falsified by `CSPACE_KEEP_ON_TEARDOWN=1` (`smoke-cspace-release-control`), under which the dead task still holds slot 0, its own `CAP_TCB`; base gate red under the same flag |
| S57 | Creating a task exercises authority the capability graph describes | `SYS_SPAWN`, `SYS_SPAWN_IMAGE` and `SYS_FORK` resolve the caller's `CAP_UNTYPED` at `CAPSLOT_UNTYPED` (`spawn_untyped_region`, `src/kernel/kspawn.c`) and `create_task` carves the child's cspace -- a `KOBJ_CNODE` -- out of THAT region. A task endowed with no untyped cannot create one; a task given a small region can spawn a bounded number of times -- which `SYS_UNTYPED_SPLIT` (**S58**) is what makes MINTABLE. When this property first landed that second clause was an overclaim: `SYS_CAP_GRANT` of a `CAP_UNTYPED` copies a capability naming the SAME region, so a delegate received its grantor's entire budget and "a small region" named something nobody could create. The gate was real and the bound was not, which is the failure mode this document exists to prevent, committed by the commit that introduced the gate. Until 2026-08-30 all five task-creating syscalls authorised on cspace slot 3 with `SC_ANYTYPE`, and `create_task` installs a `CAP_FRAME` there in EVERY task with exactly `READ\|WRITE\|EXEC`, so by **S28** that was not a gate: it was [C-1]'s decoy one table over, and every task in the system could spawn. It conferred nothing (a spawn endows a child only from what the spawner holds, **S41**/**S42**), so it was dead weight rather than a hole -- but two comments described it as enforcement. **The charge is the allocation**, not a counter beside it: the bytes the child's cspace occupies are the bytes the parent paid, so there is no second description to drift. `SYS_EXEC_NAMED` and `SYS_EXEC_IMAGE` are deliberately NOT untyped-gated -- they replace the CALLER's own image, create no task and touch no capability (**S42**), so a region they do not consume would be a second vacuous check in place of the first; they are `SC_NONE` and self-only. Kernel-initiated creation is charged to `UNTYPED_KERNEL`, which is not delegable and is sized at exactly `MAX_TASKS` cspaces, so no ring-3 allocation pattern can starve task creation. A fixed slot is legitimate here where slot 3 was not: slot 3 is ALWAYS occupied by a capability the kernel installs itself, so its test could not fail, where `CAPSLOT_UNTYPED` is empty unless somebody delegated into it and the type is checked | `make smoke-proc`, whose `grantee` child is spawned by `proctest` and deliberately NOT endowed with untyped -- it is exactly "a task spawned without the right to spawn further tasks" -- and requires `SYS_SPAWN`, `SYS_FORK` and `SYS_SPAWN_IMAGE` to return `SYS_ERR_PERM`. That error specifically, because a spawn can fail for want of a slot or a bad name and neither says anything about authority. Falsified by `SPAWN_SLOT3_DECOY_GATE=1` (`make smoke-proc-spawn-decoy-control`), which restores the slot-3 check: the un-endowed child then spawns and reports `PROC_SELFTEST: FAIL spawn-without-untyped`; base gate red under the same flag |
| S58 | A budget can be subdivided, not only shared | `SYS_UNTYPED_SPLIT(src_slot, dest_slot, bytes)` (`untyped_split`, `src/kernel/untyped.c`) carves `bytes` off the region the caller's `CAP_UNTYPED` names and mints a `CAP_UNTYPED` over the sub-region. **The parent pays**: its watermark advances past the carve before the child capability exists, so two tasks holding parent and child cannot together carve more than the parent could alone, and that holds transitively through further splits -- it is one bump allocator each time. The child capability is **derived**, not a root: own serial, the parent capability's serial as `badge`, so revoking the parent sweeps it (**S3**/**S4**); a fresh root here would be finding 3.3's shape, authority outliving the authority it came from. Rights are the parent's and are never widened, so a READ-only untyped splits into a READ-only sub-region that can be inspected and passed on but not spent. The region is **not** returned when the child dies: that is the monotonic bump discipline every other allocation here obeys, and reclaiming bytes a stale capability might still name is the type-confusion that discipline exists to forbid -- a split is a permanent transfer of budget rather than a loan. This is what makes **S57**'s bound expressible: before it, granting a `CAP_UNTYPED` named the same region, so "give this task a small budget" was a sentence with no implementation | `make smoke-captest` section 14, which splits a sub-region, checks the child is the size asked for and born unspent, checks the child is **retypable** (a region that cannot be spent is a budget in name only), and checks `after.free + child.size <= before.free` -- the parent shrank by at least what the child got. It also requires a REFUSED split (oversized) to leave the caller's free bytes unchanged, and refuses an empty source slot, a wrong-type source, a kernel-reserved destination and a zero length. Falsified by `UNTYPED_SPLIT_FREE_BYTES=1` (`make smoke-captest-split-control`), which stops the parent's watermark advancing so the split mints memory rather than spending it: `CAPTEST: FAIL split-did-not-charge-the-parent`, base gate red under the same flag |
| S59 | A supervisor can provision a server on an endpoint it created | Every server `init` launches is delegated an endpoint the KERNEL minted into `init`'s cspace before ring 3 existed (`FS_EP_REQ`, `CON_EP_REQ`, installed from the root cnode by `kshell.c`). `launch_dev_server` (`userspace/init.c`) is different in the way a mount table needs: it retypes a `KOBJ_ENDPOINT` out of the untyped region `init`'s own `CAP_UNTYPED` names, so **the object does not exist at boot** and the capability naming it is derived from that untyped rather than installed by the kernel. `init` hands the listen right (`READ`) to the server it spawns and keeps a `WRITE`-only mint for itself. A supervisor that can do this can bring up a filesystem server nobody anticipated at build time, which is what "provision a mount" means (roadmap 2.4). **`docs/ROADMAP.md` asserted `init` could not do this**, giving as the reason that it *"holds no `CAP_UNTYPED`"* -- it holds one, and had since `kshell.c` began installing it; the claim was stale rather than subtle, and it is why the item read as blocked on "a real widening" that had already happened | `make smoke-init-provision`, which drives an `FS_OP_STAT` on the server's root inode across the endpoint `init` made -- the same operation `hvfs_mount` performs to decide whether a mount can be installed, so a server that answers it is one a mount table can mount. **`init` acts as the client itself**, which is the tightest statement available rather than a shortcut: the server got `READ` and `init` kept `WRITE`, so a completed round trip proves both halves reached the task that should hold them, and proves `init` did not keep the receive right -- a request it could dequeue would answer itself. Falsified by `INIT_PROVISION_NO_UNTYPED=1` (`make smoke-init-provision-control`), which retypes from a slot holding no `CAP_UNTYPED` -- the state 2.4 asserted was permanent -- and the provisioning stops at step 1 with the server never reachable; base gate red under the same flag |
| S60 | A capability lookup refuses a type-mismatched capability | `cap_lookup` takes the EXPECTED TYPE as an argument and refuses unless the slot holds it, so a capability of the wrong type does not resolve at all. Until 2026-08-31 the resolver took only `(slot, rights)` and returned whatever the slot held; the type was tested afterwards, by each of ~40 callers. **Every live caller did test it**, so this closes no hole that was ever open — an audit of all 40 found the untested ones to be comments, self-tests, and code inside control-arm `#ifdef`s that never ship. What it removes is the requirement to REMEMBER, which is the difference between a property that holds and one that holds by construction. The dispatch table's own `c->type != d->ctype` test was folded into the same call, so a syscall's declared type is now enforced where the capability is resolved rather than beside it: two expressions of one rule were two things to drift. `CAP_ANYTYPE` is the explicit opt-out and every use carries a comment saying why — delegation from a source slot of any type, cspace enumeration, re-resolving a slot whose identity a snapshot already fixed, and two deliberately non-authorising TOCTOU snapshots in the IPC path | `make smoke-captest`, whose wrong-type refusals now witness the resolver rather than the caller. Falsified by `CAP_LOOKUP_TYPE_UNCHECKED=1` (`make smoke-captest-lookup-type-control`), which restores the untyped resolver: captest then reports `FAIL notification-cap-authorised-endpoint-recv` — a `CAP_NOTIFICATION` authorising an endpoint receive, which is type confusion named on the wire. Base gate red under the same flag |
| S61 | Several passwords open one volume, and revoking one revokes exactly that one | The volume key is wrapped once per KEY SLOT — up to `HORUS_KEYSLOTS` (8) independent AEAD wraps of `disk_key[32] \|\| uid[4]`, each under a KEK derived from its own password and its own Argon2id salt. Until 2026-08-31 there was exactly one wrap, so exactly one password could ever open a volume and a second user's `storage_unlock` return value was discarded; `docs/LIMITATIONS.md` 2.6 recorded that as orthogonal and pre-existing. **The uid is sealed INSIDE the slot**, so a stolen disk shows how many slots are active and nothing about whose they are — and a slot that opens says who opened it, which is what lets login become unlock-then-identify rather than the reverse. Two consequences are forced rather than chosen, and are documented as such: revocation is BY SLOT INDEX, because finding a user's slot by uid would need that user's password; and the last active slot cannot be removed, because a volume with no slots is not deleted but unreachable, which looks like success. **The cryptography is unchanged** — the same `derive_kek` and the same AEAD seal/open as the single wrap it replaces, only more of them, deliberately, because 5.4 records unaudited from-scratch crypto as this tree's largest real risk | `make smoke-keyslots`: two boots on one disk image, where boot 1 formats under password A and adds a slot for B, and boot 2 requires **both** to still open the volume, then revokes B and requires B refused and A untouched. Boot 1 also requires B to open in the boot that added it, so a boot-2 failure cannot be read as "never worked". Falsified by `KEYSLOT_REMOVE_NOOP=1` (`make smoke-keyslots-control`), a revocation that reports success and leaves the wrap openable: the marker is `revoked-password-still-opens-the-volume`, the PASSWORD rather than the slot count, because the count drops either way and a count-based check passes under the defect |
| S62 | User accounts survive a reboot, and an unauthentic account table refuses logins | The user table is sealed under HKDF(`disk_key`, "horus-users-v1") in a reserved region and written **through the write-ahead log**, so a crash leaves it wholly before or wholly after. Accounts were previously seeded from compile-time constants every boot and lost at power-off. Storage was never the whole obstacle: `strong_password_hash` mixed in `kernel_pepper`, sixteen fresh random bytes per boot, at both set and verify time, so a hash from one boot could not verify in the next *whatever it was stored in* (`docs/LIMITATIONS.md` 2.6 reason 3). The pepper is gone from account hashes and encryption at rest does that job instead — which also works on a machine with no TPM, unlike the superseded proposal to seal the pepper. The per-user random salt stays; `kernel_pepper` itself stays and still keys the audit chain, which is per-boot on purpose. A table that is **present and does not authenticate** sets a tamper flag and every login is refused: treating a bad tag as an empty volume would reseed the compiled-in `root` password, handing anyone who can scribble on the region a downgrade to a known credential | `make smoke-users-persist`: two boots on one image, where boot 1 adds an account and requires its password to verify **in the boot that set it**, and boot 2 requires the password to still verify **and** a wrong one to still be refused — the second half because "it verifies" alone is satisfied by a kernel that accepts everything. Falsified by `USERS_PEPPER_PER_BOOT=1` (`make smoke-users-persist-control`). `make smoke-users-tamper` is the adversarial arm: it flips a byte of the ciphertext on the platter and requires refusal **and** that no account was restored |
| S63 | A login is not consent to format a disk | Meeting an unformatted ATA volume at the login prompt used to run `storage_format_sealed` on the strength of whatever password had been typed, making it key slot 0. A mistyped password on a machine whose disk the kernel did not recognise destroyed that disk, and nothing said so. Recognising a volume is not the same as owning it, and failing to recognise one is not grounds for destroying it. Formatting is now a deliberate act — `storage_authorize_format()`, which an installer calls and a login never does. **That sentence described an intention until 2026-09-01 and a mechanism afterwards**: the function had no caller at all for as long as there was no installer, so the refusal was absolute rather than deliberate, and "no path exists" is a weaker claim than "one gated path exists". `SYS_STORAGE_FORMAT` is now its one caller and answers to `CAP_STORAGE_FORMAT` (**S72**); this property is unchanged by that and is what makes the login half of it enforceable rather than incidental. **The ephemeral vdisk is unaffected**: it is formatted inside `storage_init` with a per-boot throwaway key and never reaches this path, so a diskless boot still comes up unattended | `make smoke-storage-noformat`, which boots a blank ATA image and requires `NOFORMAT_SELFTEST: REFUSED`. Falsified by `STORAGE_AUTOFORMAT=1` (`make smoke-storage-noformat-control`), which requires `NOFORMAT_SELFTEST: FORMATTED`. **Both arms assert which branch was taken**, positively: the first version of this pair required the refusal message to be absent in the control, and passed vacuously, because the boot stops at the login prompt and neither arm ever called `storage_unlock` at all |
| S64 | A block device accepts only blocks it has memory for | The RAM vdisk advertised `BLOCKS_PER_DISK` blocks — the ceiling on the largest volume the crypto-metadata array can describe — over a backing store of `VDISK_BLOCKS`. The two were one number until 2026-08-31, when raising `BLOCK_SIZE` to 4 KiB split them so that a 4 KiB block size would not turn the RAM disk into 128 MiB of physical-pool reservation as a side effect; the device's advertised size was left behind. Blocks 4096 and above were therefore accepted and written 4 KiB at a time into the FREE PAGE POOL that begins immediately after the reservation — reachable from ring 3 by writing enough file data on any diskless boot, which is every CI boot. The transport now bounds against `vd->block_count`, a property of the memory that exists, as well as against the geometry the device advertises, and `total_blocks` is `VDISK_BLOCKS` | `make smoke-vdisk-bound`, which requires **both** that the last in-range block is still writable and that the first out-of-range block is refused — an arm asking only for the refusal is satisfied by a bound that rejects everything. Falsified by `VDISK_TOTAL_UNBOUNDED=1` (`make smoke-vdisk-bound-control`), whose marker is the pattern **read back out of the page pool**, not a missing refusal: an assertion of absence is satisfied by a run that never reached the code |
| S65 | A metadata update in a committed journal transaction is durable, whether or not its cache line was evicted first | The per-block crypto metadata (nonce, tag, present) is no longer a complete in-RAM mirror of the volume — `g_block_meta[BLOCKS_PER_DISK]`, 32 bytes per block whether or not the volume was that large — but a **bounded write-back cache** of `META_CACHE_LINES` on-disk metadata blocks, resident cost fixed regardless of volume size. The rule the structure has to carry is that **a dirty line is written back into the journal transaction that dirtied it, before that transaction commits**: `journal_commit` flushes, `journal_abort` discards, and eviction writes back as a backstop. Two consequences, and both are the point — no line is ever dirty across a commit, so a crash cannot lose a metadata update a committed transaction promised; and the metadata write is in the same atomic unit as the ciphertext it describes, so a crash never leaves a data block the journal replayed beside a nonce it did not. **A complete mirror was self-healing against a lost metadata write** — it held every entry and the next flush regenerated the lost one — and a bounded cache removes that property, so the journal is now the only thing making it safe. That is a cost of the change, recorded as one | `make smoke-meta-crash`: two boots on one image, boot 1 writing a 400-block working set that spans more metadata blocks than the cache has lines and crashing with the last block committed but not applied, boot 2 replaying and reading every block back byte-for-byte. **Both boots also assert `meta_cache_evictions() != 0`**, because a working set that fits the cache never evicts and would pass while testing nothing. Falsified three ways: `META_CACHE_NO_WRITEBACK=1` (`smoke-meta-crash-control`, block 0 lost — nothing reached the disk at all), `META_CACHE_WB_OUTSIDE_TXN=1` (`smoke-meta-crash-txn-control`, block 399 lost — precisely the block the crash committed, which is what E2 looks like), and `smoke-meta-crash-vacuity-control`, which removes the cache-shrinking widener and requires the gate to REFUSE TO CONCLUDE rather than pass |
| S66 | A metadata block is served only when it verifies against the path to the **current** rollback root — an earlier state of this volume is not a valid state | The rollback MAC is a **Merkle tree** over the metadata region: level 0's hashes are the metadata blocks' MACs, level k+1's are the hashes of level k's node blocks, and the top block's hash is `sb.meta_root`. Every hash covers (tag, level, index, bytes) — so two nodes cannot be swapped — **and** is checked against the value its parent records, transitively up to the root. The parent chain is the part that matters and the part a design can most easily omit: position binding alone still accepts a node that was genuinely valid at an earlier time, and a physical attacker rewinding part of the region writes bytes this volume really did produce. Nothing they write is forged. What it replaces was `sb.meta_hmac` over a whole-volume array of per-block MACs — 1 MiB of `.bss` hashed in full on **every metadata write** at a 16 GiB volume; a write now costs four hashes, and unlock verifies one node instead of reading the entire region | `make smoke-merkle-replay`: **three boots on one image**, with the host restoring a genuine past state of one metadata block *and the level-0 node that recorded it* between boots 2 and 3 — the pair is internally consistent, so the refusal cannot come from a hash that failed. The harness refuses to reach boot 3 unless boot 2 provably changed both blocks. Falsified twice, from the two places the rule can be lost: `MERKLE_NODE_TRUST_CACHED=1` (`smoke-merkle-replay-control`) sets a node's `verified` flag where the line is *filled* rather than where it is checked, so residency becomes the trust criterion; `MERKLE_SKIP_PARENT_BIND=1` (`smoke-merkle-parent-bind-control`) checks the leaf against the node recording it and never places that node under the root. Both markers are **positive** — the volume served a rolled-back block, 6 of 6 — not an absent refusal |
| S67 | `fsck` does not free the blocks of a live file | `storage_fsck_pass` reclaims data blocks the bitmap marks allocated but no live inode references — the repair for a crash between allocating a block and linking it. It built that reference set from `direct[]` and the single-indirect block **and stopped**, never walking `double_indirect`, so every block reachable only through the double-indirect tree, and the pointer blocks under it, were marked free **at every unlock** and handed to the next caller that allocated. Reachable by any file over 12 + `PTRS_PER_BLOCK` blocks — 2.048 MiB at 4 KiB, and **38 KiB before the block size was raised** — that survives to a second mount. The comment above the walk said "direct/single-indirect pointers", an accurate description of incomplete code, which is why it read as deliberate. The walk now descends every level, with per-level static buffers rather than 4 KiB stack frames | `make smoke-fsck-refs`: two boots on one image, boot 1 writing a file that reaches the double-indirect tree (asserted against the inode, so a run that stayed shallow fails rather than passing vacuously) and boot 2 asking the **block bitmap** whether those blocks survived the fsck that unlock just ran. It asks the bitmap rather than reading the file back, because a freed-but-still-referenced block reads back perfectly until the allocator collides with it — a read-back witness would pass over the defect. The consequence is checked too: a freshly allocated block must not be one the file owns. Falsified by `FSCK_SHALLOW_REFS=1` (`make smoke-fsck-refs-control`), which reports **4 of 4 blocks of a live file freed** |
| S68 | A volume is laid out against the disk it is on, and never larger | `g_ata_bd.total_blocks` was the compile-time `BLOCKS_PER_DISK`, so every ATA volume was laid out as though the medium held exactly that many blocks whatever it actually held — wrong in **both** directions: a smaller disk got a filesystem whose data region ran off the end of it, and a larger one was silently truncated to the constant with nothing said. The size now comes from IDENTIFY words 60–61 at probe time, clamped to `BLOCKS_PER_DISK` (which is a *ceiling* — the largest volume this kernel's fixed-size arrays can describe — and is the LBA28 wall's business, asserted at compile time). A disk reporting fewer sectors than a volume already on it is **refused at mount** rather than served in part: that is a truncated copy of a filesystem, and mounting it would serve some of one and call it whole | `make smoke-fs-16g`: three boots on a sparse 16 GiB image — format and write a file whose offsets are past the old 1.00 GiB ceiling; reboot, then commit a write and crash before applying it home; replay and read everything back — asserting **positively** that the volume is at least `BLOCKS_PER_DISK` blocks, so a gate for a 16 GiB volume cannot be satisfied by a 128 MiB image. Every other storage gate now runs on a 128 MiB image against the same 16 GiB-capable kernel, which is the property in its own right. The refusal has its own witness: `make smoke-fs-shrink` formats a 128 MiB volume, has the **host** truncate the image to 32 MiB, and requires the refusal — counted, not inferred from a volume that failed to come up. Falsified by `STORAGE_MOUNT_ANY_SIZE=1` (`make smoke-fs-shrink-control`), whose marker is that the truncated volume **mounted** |
| S69 | A sector transfer happens only when the drive says it is ready; a read that did not happen is never reported as one that did | `ata_wait_busy()` returned **void**, and its comment claimed that "on timeout the caller's status check sees BSY/0xFF/ERR and treats the operation as failed". That is true of the probe, which tests 0xFF and 0x00 explicitly, and was **false of the sector paths**: BSY is `0x80` and `ata_read_sector` tested only `0x01` (ERR). A wait that reached its bound therefore left BSY set, ERR clear, **DRQ never checked at all** — and the driver read 256 words out of a drive that had not said it had any, returning garbage **as success**. `ata_write_sector` pushed 256 words the same way, and neither tested DF. The AEAD would catch a garbage *data* block, but **bitmaps, inode tables and indirect blocks are not authenticated**: a garbage inode-table read is acted on by `storage_fsck_pass`, and a garbage indirect block yields wrong physical block numbers. The rule is now one predicate — transfer only when DRQ is set and BSY, ERR and DF are clear — and every wait's return is checked; a refusal is reported on the wire and counted | `make smoke-ata-ready`, which checks the predicate **as a pure function over all 256 status bytes** and requires exactly the 16 with DRQ set and BSY/ERR/DF clear. Tested that way deliberately: the statuses that matter are the ones a working emulator never produces, so an integration test would pass over every case the rule exists for — the same reason `sched_domain_switch_would_flush` is a pure function. Falsified by `ATA_READY_ERR_ONLY=1` (`make smoke-ata-ready-control`), which accepts **128 of 256**, the first being `0x00` — a drive that said nothing at all |
| S70 | A volume older than the machine it is presented to is refused — **whole-volume rollback** | S66's tree catches a subtree rewound while the rest of the volume moves on. It cannot catch superblock, metadata and tree replaced **together** with a consistent earlier snapshot: every internal relationship holds, every byte was genuinely produced by this volume with this key, and the root lives in the superblock it is meant to protect — so rewinding both is self-consistent by construction. Nothing inside the disk can tell "this volume" from "this volume, last week". The anchor is therefore **outside** it: a **TPM NV monotonic counter**. `sb.rollback_gen` records the counter value the volume was last written at and is **bound into the Merkle root's preimage**, so it cannot be edited without a key the attacker does not have; unlock refuses a volume whose generation is behind the counter. The counter is not a secret and does not need to be — monotonicity is the whole requirement, and `TPM_NT_COUNTER` gives it structurally: it cannot be decreased by anyone including the owner, and a re-created index starts above every value any counter on that TPM has held. The generation is written **before** the counter is raised to meet it, so a crash between them leaves `gen == counter + 1` — the one state above the counter that can exist, accepted and completed on the next boot rather than bricking the disk | `make smoke-rollback`: **three boots on one image with a kept TPM state directory**, the host imaging the disk after boot 1 and restoring **the entire image** before boot 3 — which is the attack, not a model of it. It asserts positively that boot 2 both changed the image and *advanced the generation* before restoring, so a run that replayed nothing cannot pass. Falsified by `ROLLBACK_ANCHOR_IGNORE=1` (`make smoke-rollback-control`), where boot 3 mounts and reports `found era 1` — the state the machine had already left, where a current volume holds era 2. The anchor itself has its own gate, `make smoke-nvcounter`, because a counter that could repeat a value would let a rolled-back volume match |
| S71 | A record the kernel copies into ring 3 has **one** definition, and both rings compile it | `struct audit_event` was declared twice — 256 bytes and twelve fields in `src/include/kernel.h`, 72 bytes and seven in `include/syscall.h` — under one name, for one syscall. `h_read_audit` copied `sizeof(struct audit_event)`, the kernel's 256, at the kernel's stride into an array ring 3 had sized at 72 an element. Both consequences are real and only one of them is visible to the caller: **every field was read from the wrong offset** (a caller's `subject_uid` was a kernel field nothing writes, so it read zero — an audit log that answers "who" with 0 is worse than one that errors), **and the write ran off the end** — `max * 256` bytes into `max * 72` bytes of caller memory, 184 past the array per record. `userspace/grantee.c` asks for 2 into a 144-byte **stack** array, so the kernel wrote up to 512 bytes there on every `PROC_SELFTEST` boot since that caller was written. `user_copy` walks the caller's own CR3, so this is confined to the caller — corruption, not a privilege boundary, exactly as issue #176 was — and the same sentence applies: *the kernel wrote to an address the caller did not name*. The exported record is now `struct audit_record` in `include/audit_abi.h`, included by **both** rings, with a `_Static_assert` on its size that both rings compile; the kernel's internal event stays private and is projected onto it by `audit_export()`, which zeroes the whole record first because it is a kernel stack object. It is deliberately not the internal struct: `kind`, `uid`, `arg0`, `arg1` and `path` are written by nothing, and exporting a field is a promise to keep filling it. The arrangement, and the reason for it, is `include/block_size.h`'s one subsystem over | `make smoke-auditprobe`, whose probe reads records back into an array with a guard region immediately after it and requires the guard untouched — the half no return code can report — and separately requires the fields to land where the caller declared them, anchored on `subject_uid == 1000`, which only this task is. Falsified by `AUDIT_ABI_LEGACY=1` (`make smoke-auditprobe-abi-control`), which restores **both** declarations and the raw kernel-sized copy, because the defect is the disagreement rather than either side: marker `AUDITPROBE: FAIL read-audit-wrote-past-the-array`, 3 boots in 3, with the misread-field failure printed beside it. **Found by the first thing that ever entered the handler** — see `docs/LIMITATIONS.md` 1.8, which predicted exactly this |
| S72 | Destroying a volume answers to a capability nothing else in the system holds | **S63** made formatting a deliberate act -- `storage_authorize_format()`, "which an installer calls and a login never does" -- and then left that function with **no caller at all**. "No path exists" and "one gated path exists" are different claims and only the second is a policy, so the refusal was absolute rather than deliberate for as long as there was nothing to be deliberate. `SYS_STORAGE_FORMAT` is now its one caller, and it answers to `CAP_STORAGE_FORMAT` at `CAPSLOT_STORAGE_FORMAT`: a capability **type** of its own, held by `init` from the primordial root cnode and granted onward to the installer alone. `SYS_STORAGE_INFO` -- the "what will be destroyed" readout -- takes the same capability with `READ` where the format takes `WRITE`. **It is deliberately not a rights bit on `CAP_ENCRYPTED_STORAGE`**, which was the first design and is the wrong one for a reason the tree shows rather than argues: `root_cnode[9]` carries `CAP_RIGHT_ALL`, `cap_install_from_root` copies rights verbatim, and existing call sites hand a full copy of that capability to `fs_server` and to the shell -- so a new bit inside `CAP_RIGHT_ALL` is conferred on both the moment it is defined, with no diff at the grant. Formatting would have become something the filesystem server and the login shell could do because of how a constant is spelled. A new type fails closed instead: a slot holding no `CAP_STORAGE_FORMAT` authorises nothing whatever rights it carries. That is **S18**'s split of `CAP_DEBUG` out of `CAP_AUDIT` applied before the bundling exists rather than after. The primordial carries `READ\|WRITE` and **not** `CAP_RIGHT_ALL`: rights only narrow on delegation, so a capability that never held `GRANT` or `MINT` cannot produce a descendant that does. The password is bounded at `STORAGE_FORMAT_PASSWORD_MAX` (31) and an over-long one is **refused, not truncated** -- a login copies 31 bytes and offers exactly those to `storage_unlock`, so a volume sealed to more could never be opened by the operator who chose the password | `make smoke-captest` section 15: a task holding no `CAP_STORAGE_FORMAT` is refused `SYS_STORAGE_FORMAT` and `SYS_STORAGE_INFO` with exactly `SYS_ERR_PERM`, the info buffer is unwritten on the refusal, and a **wrong-type** capability minted into the gate's own slot is refused too. Falsified by `STORAGE_FORMAT_UNGATED=1` (`make smoke-captest-storage-format-control`), which removes the whole dispatch row -- slot, rights and type -- so the call SUCCEEDS and the marker `CAPTEST: FAIL storage-format-without-cap-storage-format` appears; base gate red under the same flag. The whole row goes rather than half of it, because either half alone still refuses and an arm against one would pass for the wrong reason. The **positive** half is `make smoke-storage-survey`: two boots of the same kernel, one with a blank disk attached and one without, requiring `init` -- which does hold the capability -- to report `disk present` in the first and `no persistent volume` in the second, each arm requiring the other's marker to be absent. Without it these refusals are all satisfied by a syscall that refuses everyone |
| S73 | A disk is erased only after an act that means erasing the disk and nothing else | The installer holds `CAP_STORAGE_FORMAT` (**S72**) and could format the moment it starts. What stops it is not the capability but a **typed word**: the operator must type `FORMAT` into a field, and anything else -- including an empty field, including `yes` -- cancels with nothing written. **A menu is not consent.** The menu in front of it defaults to Cancel, and defaulting to Cancel is worth having, but a menu whose default is Cancel still becomes a format with two keystrokes, and a person holding return through a wizard is not a person who has decided. A word cannot be reached by any sequence of keys that is not the word. This is **S63** one layer up and in the same direction: there, a password typed at a login prompt was taken as consent to format a disk the kernel did not recognise; the repair was to require an act that means only that, and this is what that act looks like when a person performs it. The installer also refuses outright to install over a **recognised** volume -- that is not offered as a choice, because overwriting a filesystem somebody may still want is a different act needing a different confirmation, and putting the two in one menu is how they get confused | `make smoke-installer-refuse`, which answers the confirmation with `yes` and requires `INSTALLER: nothing was written` **present** and `INSTALLER: formatting` **absent** -- both directions positively, because an absence alone is satisfied by a run that never reached the installer, which is exactly how the first version of the S63 pair passed vacuously. Falsified by `INSTALLER_NO_CONFIRM=1` (`make smoke-installer-refuse-control`), which drops the comparison and nothing else, so both arms type the same wrong word and differ only in what the program does about it: the control arm requires `INSTALLER: formatting` to be **present**. The positive direction is `make smoke-installer` |
| S74 | The object store answers only an **unlocked** volume, not merely a mounted one | A volume has two states that both look like "there is a filesystem here": `mounted` (the superblock is read and verified, so the geometry and the metadata root are known) and `unlocked` (a key slot has been opened, so `disk_key` exists and the AEAD can run). A **sealed ATA volume is mounted and locked** for the whole of every boot until a login unlocks it -- the normal state of an installed machine between power-on and its password prompt, not an edge case. The eight object-store handlers tested only the first: `SYS_FS_STAT`, `SYS_FS_INODE_ALLOC`, `SYS_FS_INODE_FREE`, `SYS_FS_INODE_LINK`, `SYS_FS_SET_SIZE`, `SYS_FS_SET_META`, `SYS_FBLOCK_READ` and `SYS_FBLOCK_WRITE`. **The AEAD hid half of it**: `storage_decrypt_block` and `storage_encrypt_block` both test `unlocked` and fail closed, so the two file-DATA calls were saved downstream by the layer that needs the key. The **inode table is plaintext on disk** -- `storage_read_inode`/`storage_write_inode` go through `do_block_write` with no crypto at all -- so nothing caught it there, and a sealed volume served real inode records (size, mode, uid, gid, type, links) and accepted edits to its own inode table. The half of the API with a key requirement enforced the rule as a side effect of needing the key; the half without one enforced nothing, which is why it read as correct. The gate on all eight is `CAP_ENCRYPTED_STORAGE`, so it was never reachable from an ordinary ring-3 task -- but **that is a statement about who, and this rule is about when**. A capability is not a password. The check now lives in **one place**, `store_open()`, for the reason **S60** folds the type check into `cap_lookup`: eight handlers repeating a predicate is eight chances to write the one that was already written wrong eight times. `SYS_ERR_INVAL`, matching the unmounted case it now shares a return with -- both mean "there is no open store to act on", which is about the volume and not the caller, whose authority the dispatch table settled first. The **raw block API is deliberately unchanged**: `h_block_read`/`h_block_write` sit below the volume abstraction because the journal and crash gates need them there, and a raw read of an encrypted block discloses nothing a stolen disk does not | `make smoke-installer-provision`: install onto a bare disk, **power the machine off at the login prompt without logging in**, and require the next boot to finish the job -- `FS_STORE: sealed at startup; provisioning deferred until unlock`, then a login, then a `/bin` that lists the provisioned binaries. The power-off is the experiment rather than a shortcut: `fs_server` copies the base system in when the volume unlocks, so an install nobody logs into leaves that copy unfinished, and this is the only boot on which the property is observable -- and one every real install passes through. Falsified by `STORE_LOCKED_UNCHECKED=1` (`make smoke-installer-provision-control`), where the same disk in the same state reports `FS_STORE: open at startup; provisioning now` and `/bin` is **empty on that disk for good**: `fs_server` decides at startup whether the store is usable by asking for the root inode, got yes on a sealed volume, ran the copy against a locked store where every data write failed inside the AEAD, and recorded itself provisioned anyway -- which permanently disabled the post-login retry its own comment describes as the sealed-ATA fallback. **Both arms assert a marker positively** -- which branch `fs_server` took -- rather than the absence of one, because an absent `/bin` alone is satisfied by a run that never reached a shell. Falsified in the other direction too: the base gate goes red under the flag (`tools/check_base_gate_reddens.sh STORE_LOCKED_UNCHECKED`, measured 2026-09-01) |
| S75 | A password command changes the account it names, and says which one it changed | The shell's `passwd` MATCHED an argument (`strncmp(cmd, "passwd ", 7)`) and then dropped it: every call passed `sys_getuid()` to `SYS_PASSWD`. `useradd` creates an account **locked** -- `do_useradd` stores random bytes in `pass_hash` when no initial password is supplied, so no password can ever match -- and `passwd <uid>` is the only way to give it one, so a root operator following the obvious sequence typed `passwd 1001`, was told `password changed`, and had changed **their own**. The reply could not distinguish the two: it is true of whichever account was actually written. **This is a credential change, not a mis-print.** `do_passwd` re-wraps the volume key whenever `target_uid == my_uid`, so the string typed for somebody else's account became the volume's key-slot password too -- and the installer's own screen says a forgotten password is a lost volume. Measured before the fix: `passwd 1001` typing `bobpass1`, then a power cycle, and `root`/&lt;install password&gt; was **refused** while `root`/`bobpass1` opened the machine. **A malformed argument is now refused rather than ignored**, which is the actual repair: a silent fallback to self would reproduce the defect for `passwd bob` -- a name, which is what an operator who knows POSIX types first -- while the uid form worked, a worse failure than the uniform one because it teaches the wrong model. There is no name lookup to offer instead; `useradd` and `userdel` are uid-only for the same reason. The root test is the **shell's own**, for the reason its `useradd` branch gives: the kernel asks whether this TASK holds `CAP_USER`, and the shell is one long-lived task serving successive logins, so leaning on the kernel's gate alone would hand account administration to every logged-in user. `do_passwd` refuses `uid != self` for a non-admin as well, so there are two gates; this is the one that knows who is at the terminal. **What this does NOT give a new account** is a volume key slot, so it cannot be the FIRST login after a power cycle -- see `docs/LIMITATIONS.md` 2.6b | `make smoke-passwd-target`: two boots on one image. Boot 1 logs in as `root` (sealing the volume), creates an account, runs `passwd <uid>`, and requires the reply to **name that uid**, then requires `root`'s own password to still work and the new account to log in with what it was given. Boot 2 requires the volume itself to still open with the password it was sealed to -- the half that makes this a credential defect rather than a session-scoped one. Falsified by `PASSWD_TARGET_IGNORED=1` (`make smoke-passwd-target-control`), which restores the dropped argument and requires `password changed for uid 0`, a **successful** `root` login with the other account's password, and a boot 2 that opens the volume with it. **Every assertion in both arms is a login that succeeds**, never a refusal, because a refusal is satisfied by a boot that never reached a prompt. Falsified in the other direction too: the base gate goes red under the flag (`tools/check_base_gate_reddens.sh PASSWD_TARGET_IGNORED`, measured 2026-09-01) |
| S76 | An account an administrator creates can be the **first** login after a power cycle | A Horus volume is sealed to **key slots** (**S61**): up to `HORUS_KEYSLOTS` wraps of the same `disk_key`, each under a KEK derived from one password. `h_auth` calls `users_unlock_and_restore(typed_password)` **before** it consults the account table, because on a sealed volume the table it would otherwise read is the compiled-in one `users_init` seeded. So an account with no slot opened nothing, the persisted table was never loaded, the account was not found, and the login was refused -- **while working perfectly on a machine somebody else had already opened**. That asymmetry is why it survived: every test and every operator logs in as root first. It was recorded as `docs/LIMITATIONS.md` 2.6b and is now closed. `do_passwd` grants a key slot when an admin sets another account's password, and records its index in `user_account.keyslot` -- **both the mechanism and the field already existed and had no live caller**: `storage_keyslot_add` had only a selftest, and the field's one assignment was also in a selftest, the same shape as **S63**'s `storage_authorize_format` before **S72**. The **order is the fail-safe**: the slot is added before the hash changes and the old slot is removed only after the new one is written, so a failure changes nothing, the count never dips to one where `storage_keyslot_remove` refuses, and a crash leaks a slot rather than losing one -- two passwords opening a volume is recoverable, none is not. Changing your **own** password still takes `storage_rekey`, because the slot this boot opened is already yours and a second one would leave the old password valid. A machine with no persistent volume grants nothing and wants nothing, since the vdisk does not outlive the boot. `users_init` and `do_useradd` now write `KEYSLOT_NONE` explicitly: the static zero-initialiser reads as slot **0**, a valid index and the one the formatting password owns, so every account silently claimed root's slot and an admin password change would have revoked it | `make smoke-installer-accounts`: install two accounts, power the machine off, and require the **unprivileged** one to be the first login on a machine nobody has opened -- then root on a slot of its own, and then the compiled-in `user`/`password` account to be **absent** (a fresh volume's table is seeded from `users_init`'s RAM image, so before the installer deleted it every install shipped a login whose password is printed in the source). Falsified by `PASSWD_NO_KEYSLOT=1` (`make smoke-installer-accounts-control`), where the user is refused first, **root then logs in, and then the same user does** -- the third step separating "the account is broken" from "the account has no key slot". Falsified in the other direction too: the base gate goes red under the flag (`tools/check_base_gate_reddens.sh PASSWD_NO_KEYSLOT`, measured 2026-09-02) |
| S77 | A file's **mode** is set by its owner or root; its **owner** is set by root alone | Two rules, not one, and they differ because the acts differ. Changing a mode is something an owner does to their own file. Giving a file **away** is not: an owner who could hand one to another uid could leave an object under a name that account is answerable for, and an owner who could **take** one could help themselves to anything they can read. So `FS_OP_CHMOD` is owner-or-root and `FS_OP_CHOWN` is root-only, both decided against `cuid` -- the uid `SYS_IPC_SENDER` attests for the calling task, never a field of the request. **Both operations existed from the beginning with no ring-3 caller at all**: the shell had no command for either, so neither rule had ever been exercised from a real login, and a gate nothing reaches is a gate nobody has watched fail. `chmod` and `chown` are shell commands now, and the enforcement is where it already was -- the shell adds **no check of its own**, deliberately. `useradd` and `userdel` carry one because `CAP_USER` is per-**task** and the shell is one long-lived task serving successive logins; the filesystem is the other case, because `SYS_AUTH` rewrites the task's attested uid at every login, so `cuid` already is whoever is at the terminal. A second copy of the rule in the shell would be a policy kept where nothing verifies it | `make smoke-session`, in both identities: root gives a file away and sets its mode, and each change is **read back from the server** rather than inferred from the command's own reply; then a standard user is refused the chown, refused the chmod of a file they do not own, and **allowed** the chmod of a file they do. That third check is what stops the pair being satisfied by a server that refuses metadata to everybody. Falsified one arm per rule: `FS_CHOWN_ANY_UID=1` (`make smoke-session-chown-control`) and `FS_CHMOD_ANY_OWNER=1` (`make smoke-session-chmod-control`). **Each arm leaves the other refusal standing** -- measured 2026-09-02, the chown arm reports `chown ALLOWED` with `chmod refused`, the chmod arm the reverse -- which is what shows the two rules are independent rather than one rule read twice. The two refusals deliberately name **different files**: under the chown arm the user takes `note`, so sharing a target would hand them ownership of the other arm's file and both arms would pass |
| S78 | Every account has a home directory that the **account** owns, and the account table is read only with `CAP_USER` | Two halves of one gap, found on an installed machine. The provisioned skeleton (`/bin /etc /home /lib /usr /usr/share`) is created root-owned 0755 and nothing created a home, so a standard user had **nowhere writable anywhere on the volume** -- and no way to be given one, because root could create a directory but the operation that hands it over had no shell command until **S77**. `useradd` recorded `/home/<name>` in the account and no such directory ever existed. It is `fs_server` that creates them, and that is the only place in the system where the three things this needs are true at once: the **account list** (it already holds `CAP_USER`, granted by `init` as the `SYS_REGISTER_FS_SERVER` gate, so no authority is added here), the **authority to stamp ownership** (`sys_fs_set_meta` answers to `CAP_ENCRYPTED_STORAGE`, which is this server and nothing else in ring 3 -- the installer deliberately holds no storage capability and could not do it), and the right **moment** (an installed volume is sealed from power-on until a login unlocks it, **S74**, so anything attempted at boot writes nothing; provisioning is already the code that waits for the unlock). An existing directory is left **entirely alone** rather than re-stamped: re-asserting 0700 and the owner every boot would overrule a mode the account chose, once per power cycle. The list itself answers to `CAP_USER` at `CAPSLOT_USER`, tested in `do_userlist` beside the gate `useradd`/`userdel`/`passwd` share. It returns **metadata only** -- name, uid, gid, home -- and the handler copies **fields**, not the record, so a field added to `user_account` cannot be exported by sitting next to one that is; the buffer is zeroed before it is filled, because `kstrcpy` leaves the tail of a 32- or 64-byte array as it found it and `copy_to_user` copies the whole struct. A read-only call returning no secrets still needs the gate: *no ambient authority* is a property of the system rather than of each syscall argued on its own merits, and an ungated enumeration is exactly the ambient path | `make smoke-session`: a standard user's home is **owned by them** (asked of the server with `stat`, not inferred) and they can **create in it** -- two checks, because ownership without a usable mode and a mode without ownership each satisfy one and not the other. `make smoke-captest` section 16: a task holding no `CAP_USER` is refused `SYS_USERLIST` with exactly `SYS_ERR_PERM`, and its buffer is **unwritten** on the refusal. Falsified one arm per half: `HOME_DIR_ROOT_OWNED=1` (`make smoke-session-home-control`) creates the directory and withholds ownership, and **both** of the session's checks must then fail; `USERLIST_UNGATED=1` (`make smoke-captest-userlist-control`) removes the capability test, and the ungated read **succeeds** -- a booted system always has accounts -- which is what stops the refusal test passing under its own defect |
| S79 | **No syscall in the ship build is authorised by a capability the kernel hands every task** | `create_task` installs a `CAP_FRAME` in cspace slot 3 of every task -- `READ\|WRITE\|EXEC`, naming a fixed window, asked for by nobody -- so a dispatch row reading `{ handler, 3, WRITE\|EXEC, SC_ANYTYPE }` authorises the whole system. That is finding **[C-1]**'s decoy, and by **S28** a gate every task passes is not a gate. The property is not new; what is new is that anything **checks** it. It had been swept three times and each sweep left rows behind: **[H-3]** (2026-08-22) removed `SYS_OPEN`, `SYS_RAMFS_CREATE` and `SYS_RAMFS_LIST` and left a comment calling those "the last three"; #201 (2026-08-23) found a fourth, `SYS_EXEC_LEGACY`, invisible because its entry was the bare index `[14]`; audit 4.1 (2026-08-30) moved the five task-creating syscalls to `CAP_UNTYPED` (**S57**) and described the old gate as "vacuous". None enumerated `SYS_EXEC` (19) or `SYS_RECEIVE_PROGRAM` (27), which carried the identical row in the **ship** build until 2026-09-03. **The fact was not even unknown**: `.github/syscall-coverage.yml` had recorded it in prose against both since 2026-08-20 -- *"the slot-3 check does not stop a caller, and a successful call arms an image"* -- under a group header that asserted the same shape for three syscalls which had since been fixed. A true sentence sat beside three false ones for two weeks and gated nothing, which is the argument for a checker rather than a fourth sweep: **a fact in a reason field is not a gate.** Both syscalls are **retired** rather than re-gated, following 5, 6, 7 and 14: neither had a caller, `SYS_EXEC` dropped the caller to ring 3 at an address it chose with no validation and was superseded by `SYS_EXEC_NAMED`/`SYS_EXEC_IMAGE`, and `SYS_RECEIVE_PROGRAM`'s transport is a second serial port **no target in this tree attaches**, reached after `loader_disarm()` had already destroyed whatever image another task had staged. Retiring 27 also settles the order of a repair: `docs/LIMITATIONS.md` 2.18 records that its success path could not be reached because `struct program_header` is 104 bytes in the kernel and 44 in ring 3, and **fixing that struct first would have made a decoy-gated, disarm-first handler reachable for the first time** -- "safe because something else breaks first" is the shape **S28**, **S30** and **S38** all turned out to have, and it is not a property | `tools/check_dispatch_gates.py` (required job, in the source-only checker set) parses the dispatch table the compiler reads and fails the build on any row gating on slot 3 that is not behind a macro the ship build provably lacks -- so a row cannot come back, and cannot be smuggled back behind an unrelated `#ifdef` or into an `#else` arm. Falsified in four directions: an un-guarded row, a row guarded by an unknown macro, a row in an `#else`, and a renamed table (the self-check, because every rule above is vacuous against a regex that has stopped matching). The runtime half is `make smoke-passwd-probe`, now **10 checks**: an ordinary uid-1000 task holding nothing anyone delegated calls all six retired numbers and requires `SYS_ERR_NOSYS` from each. Falsified one arm per door, both under `LEGACY_SYSCALLS_PRESENT=1`: `make smoke-passwd-probe-recv27-control` requires `PASSWDPROBE: FAIL receive-program-reachable-on-the-slot-3-decoy`, and `make smoke-passwd-probe-exec19-control` requires **`SYSCOV 19`** -- a **kernel-side** marker, because `h_run` ends in `drop_to_ring3` and never returns, so the defect destroys its own ring-3 reporter. A ring-3 landing pad was tried first and printed three bytes before the machine went quiet. Both arms falsified in the other direction too: with the flag absent and `SYSCALL_COVERAGE=1` still set, neither `SYSCOV 19` nor `SYSCOV 27` appears and the probe reports `PASS 10 checks` |
| S80 | **The `.bin` program-image container has exactly one declaration, and the kernel reads what the build tool wrote** | The format is a 44-byte header -- `magic` at 0, `entry` at 4, `size` at 8, `name[32]` at 12 -- followed by the payload. Until 2026-09-03 it was DECLARED four times and PARSED in eleven places, and no two copies were connected. Two of the declarations shared the name `struct program_header` and described different things: 104 bytes in `src/include/kernel.h` (an ELF program header with four Horus fields appended, `magic` at offset **96**) and 44 in `include/syscall.h`. Neither was used by anything -- the `program_header_t` typedef had no uses at all and not one of the eight ELF fields was ever read. The third copy, private to `tools/mkheadered.c`, is the one that actually DEFINED the format, because that tool writes every `.bin` in the tree. And the readers used none of the three: `arm_named_binary` and `arm_image_from_user` assembled the fields from the literals 0, 4, 8, 12 and 44 with `0x55524F48` spelled out at each site. **`tools/check_image_abi.py` found seven more on its first run** -- two in `kshell.c`, on the boot path that spawns `init` and the shell, and five in `selftest.c` -- so the section that recorded this said *four copies* and the true number was eleven. That is the same shape as **S79**'s finding one file over: a count written by hand, short, and closed on. Most of those eleven also did `*(const uint32_t *)(bin + 4)`, an unaligned load through a typed pointer on a linker symbol. **What the divergence cost** is recorded rather than guessed: it broke `SYS_RECEIVE_PROGRAM` in two independent places -- the copy-out wrote the kernel's 104 bytes into a 44-byte ring-3 stack object, 60 past its end, and the wire read tested `magic` at offset 96 against payload bytes so the transfer answered *Bad magic* every time. The overrun was unreachable **because** the wire read failed first, which is why that syscall was retired (**S79**) before this was repaired: fixing the struct first would have made a decoy-gated handler succeed for the first time. There is now one declaration, `struct horus_image_header` in `include/program_abi.h`, which the kernel, ring 3 and the host build tool all include; one parse, `image_container_parse`, which all eleven sites call; and no literal offsets anywhere. The **rename** is deliberate and is S71's lesson verbatim: sharing a name was half of how the divergence survived, since both sides compiled and both were self-consistent, so a rename makes a stale caller fail to BUILD rather than fail to AGREE. The eight ELF fields are not carried over, because exporting a field means promising to keep it | `tools/check_image_abi.py` (required, in the source-only checker set) refuses a second declaration of the container anywhere in the tree **under any name** -- it matches on the field set, not the spelling -- and a second spelling of the magic. Falsified four ways: a second magic, a redeclaration under a different name, the declaration renamed away, and the header removed (the two self-checks, because both rules are vacuous against a checker whose subject has moved). `make smoke-image-abi` is the runtime half and it exists because the `_Static_assert`s cannot cover this: they prove both sides COMPILE the same layout, not that the bytes on disk came from it, `mkheadered` being a separate program built by a separate compiler. It arms a real boot module and requires name, size, entry and the payload offset to survive the round trip -- 6 checks. Falsified by `IMAGE_HDR_WRITER_SKEW=1` (`make smoke-image-abi-control`), which makes `mkheadered` emit `name` four bytes further into the same 44-byte header: magic, entry, size and the payload offset all stay correct, the image **arms successfully**, and only the name comes back wrong. That silence is deliberate -- an arm that broke the magic would be caught by any reader, and an arm that only asked *did it load?* would pass. `make smoke` plus `smoke-preempt`, `smoke-smp`, `smoke-proc`, `smoke-signal`, `smoke-tsd`, `smoke-fs` and `smoke-captest` (182 checks) all re-run green over the converted parse, which is what covers the eight sites the selftests own |
| S81 | **A kernel diagnostic cannot be cut in half by a ring-3 task's output** | The kernel reports through COM1, which a ring-3 `console_server` also owns (finding **#126**): the kernel writes it anyway, because a report that loses a race with a shell prompt is not a report. `panic_str` is `while (*s) panic_ch(*s++)`, so another task's write lands **between two characters of a single call**. On 2026-09-02, CI run `33553525177`, `kfault_str("\nPANIC: two CPUs parking on one kernel stack rsp=")` reached the wire beginning `Us parking on one kernel stack rsp=` -- ring-3 `PROC_SELFTEST` output had landed inside it, and `smoke-kstack-park-control` reported *the shared park did NOT reproduce in 8 boots* with the panic four lines below that sentence in its own evidence dump. **A gate whose marker can be destroyed by unrelated output fails OPEN**, and no lock closes it: the party the kernel must exclude is a task it does not schedule out. `tools/check_split_markers.py`'s rule -- a gated marker is one write -- is right for ring 3, where `sys_write` delivers a buffer, and cannot reach this: it IS one call, and the write underneath is a character loop. The repair is a channel with one writer end to end. **COM3 (0x3E8) is kernel-only, and by authority rather than by convention:** `src/kernel/pci.c` declares the platform device's ports -- PS/2, COM1, COM2, and `[0x3B0,0x3E0)` of the VGA register file -- and 0x3E8 is in none of them, so no capability names it and `SYS_IOPORT_GRANT` can never open the TSS I/O bitmap for it. COM1 still gets a best-effort copy, because a human running `make run` watches one line; nothing asserts on it. **`isa-debug-exit` at 0x604 was the first design and is rejected**: an exit code cannot be split either, but it terminates the machine, so it carries only a FATAL report -- and the reports that hide are the survivable ones -- and it carries a number where the text is the diagnosis. **COM3 rather than COM2**, which is equally unused, because attaching a backend to COM2 changes what `serial2_read_char` sees: an unassigned port floats and reads `0xFF`, which is why the retired `SYS_RECEIVE_PROGRAM` answers -1 under `LEGACY_SYSCALLS_PRESENT` instead of blocking, and it is what `smoke-passwd-probe-recv27-control` asserts on. **Scope, stated rather than implied:** the channel exists and is gated; the roughly twenty existing assertions on kernel-emitted strings (measured 2026-09-03 across `Makefile`, `tools/` and `ci.yml`) still read the shared console and remain exposed until they are migrated -- `docs/LIMITATIONS.md` 2.6c carries that remainder | `make smoke-kdiag` requires every marker the kernel emits to be contiguous on the diagnostic channel. Its pair, `make smoke-kdiag-split-control`, is the **same build and the same boot** read on the other channel: the kernel emitted 6 markers whole on COM3 and fewer arrived intact on the shared console -- 5 boots in 5, two on 12 host cores and three with every guest CPU pinned to **one**. **The widener waits on guest time**, and the version that did not is the measurement worth keeping: a fixed spin per character reproduced 6 boots in 6 locally and **0 in 1 on CI**, and the cause was not parallelism -- pinning every guest CPU to one host core reproduces hardest of all -- but DENSITY, the same workload emitting 134 ring-3 lines on the runner against ~6000 locally. A spin count measures the host; the hazard is a race in guest time. Ground-truthed against COM3 rather than against the console's own count, because counting there **misses the hardest reproductions**: a write landing inside the word `KDIAGPROBE` leaves no prefix to count, and a boot that shredded 7 of 8 scored *clean*. That is CLAUDE.md's park worked-example met again. `make smoke-kdiag-legacy-control` (`KDIAG_LEGACY_COM1=1`, the pre-fix reporter) requires the base gate to go **RED**, 3 boots in 3, with a verdict that names the condition rather than the generic not-a-result a broken boot gets. The authority half is a two-sided pair in which `console_server` attempts the write in **both** and only the kernel's port declaration moves: `make smoke-kdiag-ioport` requires a **#GP** (`ring-3 trap vector 13`) and a clean channel, and `make smoke-kdiag-grant-control` (`KDIAG_PORTS_GRANTABLE=1`) requires 200 ring-3 sentinels **in the kernel's own channel** and no fault -- 3 boots in 3 each. Attempting it in both directions is the difference between *ring 3 did not write the channel* and *ring 3 cannot* |
| — | Where whole-volume rollback stands | This row used to say the property was not implemented, and said so for as long as that was true. **S70 implements it** (2026-09-01): the anchor outside the volume is a TPM NV monotonic counter, `sb.rollback_gen` is bound into the Merkle root's preimage, and a volume behind the counter is refused. What is NOT covered is narrower and is stated where it can be acted on -- a volume on a TPM-less machine is unanchored, the granularity is one boot, and there is no migration path for a volume formatted before the field existed. See `docs/LIMITATIONS.md` 1.12. This row is kept rather than deleted because "we added a Merkle tree" invites the reading that rollback is solved, and the boundary between S66 and S70 is exactly where that reading goes wrong | `make smoke-rollback` for what IS covered; `docs/LIMITATIONS.md` 1.12 for what is not |

S13 and S14 were undermined until 2026-07-27 by the [C-1] defect, an attacker could impersonate
the server rather than lie to it. S13a and S13b are the properties that now hold the transport
up, and are tested by the refusal checks in `smoke-captest`.

---

## Cryptography

| Purpose | Primitive |
|---|---|
| CSPRNG | ChaCha20, fast-key-erasure construction, seeded from RDRAND (health-checked), TSC, and interrupt jitter |
| Data at rest | AEAD with per-`(inode, block)` subkeys and a fresh nonce per write |
| Hashing / measurement | SHA-256 (TPM PCRs, boot-module manifest), BLAKE2b |
| Password hashing | Argon2 |
| Key derivation | HKDF for the vdisk KEK |
| Integrity | Hierarchical rollback MAC over block metadata |

All primitives are implemented in `no_std` Rust in `rust/src/`. They are **not** independently
audited and **not** verified constant-time. The CSPRNG replaced an earlier LCG-plus-raw-TSC
construction that was predictable from ring 3.

---

## Process security

The engineering environment is part of the trusted base. Current state:

**In place:** signed commits enforced by ruleset; protected `main` with no bypass actors; linear
history required; force-push and deletion blocked; all third-party GitHub Actions pinned to full
commit SHAs; least-privilege `GITHUB_TOKEN` scopes per workflow; 22 required status checks
(including, since 2026-08-15, the capability conformance suite that witnesses most of the table
above) with `strict_required_status_checks_policy: true`, so a stale-base merge is no longer
permitted; reproducible-build verification; CodeQL, Semgrep, Trivy, gitleaks, cargo-audit;
CycloneDX SBOM per run; Dependabot on Actions and Cargo; secret scanning with push protection;
cargo-fuzz on the FFI boundary; Kani proofs on capability revocation.

**Known gaps**, all tracked in the audit:

- **Merges require no reviewer approval** (`required_approving_review_count: 0`). Every
  recent pull request merged with zero reviews. For a single-maintainer project this is a
  structural limitation, and it is stated here rather than glossed over: **the assurance
  Horus can currently claim is "thoroughly automatically verified", not "independently
  reviewed".** Finding **[C-5]**.
- **Which CI jobs gate a merge cannot be verified by CI itself.** Finding **[C-6]**, and what
  remains of it is narrow. *This bullet said "most security-specific CI jobs are **not**
  merge-gating … the rest are still advisory, and the mechanism that produced the omission is
  untouched" for a day after all three of those clauses stopped being true. Corrected
  2026-08-17.*

Every security gate now blocks a merge, kernel W^X, measured boot, module and newlib tamper
rejection, SMEP/SMAP, flush-on-switch, stack-guard reseed, the 64-bit heap, interrupt policy,
per-CPU identity, the resume-`%rsp` guard, both S20 kernel-stack gates, the SMP soak and CodeQL.
`.github/ci-gating.yml` is the checked-in decision record and the `ci-gating` job fails the
build if any job is unclassified, double-classified, or names a job that no longer exists; there
is no default, because defaulting is what produced the finding. The ruleset required **22**
contexts before 2026-08-16.

  **What keeps it open:** reading a ruleset needs the `Administration` permission, which is not
  among the scopes a workflow `GITHUB_TOKEN` can be granted, so `ci-gating` proves the
  classification is *complete* and not that the ruleset *matches* it. A scheduled
  `ruleset-audit` job closes that by authenticating as a GitHub App with `Administration: read`
  scoped to this repository. **Live since 2026-08-19**, it now reads the ruleset and states the
  comparison, having failed loudly rather than skipped for the days it was unconfigured. Syncing
  the ruleset remains a manual step that must lag a job landing by one merge, which is what is
  left of this finding. Read the count from
  `gh api repos/pharanyx-labs/Horus/rulesets/21815299`, not from this sentence.
- No build provenance attestation or signed release artifacts. Finding **[I-9]**.

If you are evaluating Horus, weigh those gaps against the claims in the table above.

---

## Supported versions

Horus has no tagged releases yet. Security fixes land on `main`. Anyone deploying Horus for
research should track `main` and rebuild.

---

## Hardening the build

- Ship with default flags. `DEBUG_SHELL=1` enables an in-kernel debug shell and additional
  syscall surface; it is a development aid, not a shipping configuration.
- `PREEMPT_SELFTEST` and the other `*_SELFTEST` builds add test-only syscalls that are
  absent (and fail closed) in the default kernel.
- Verify reproducibility before trusting a binary: run `make reproducible-build` twice and
  compare the `kernel.elf` line of `.build.sha`. A single run records hashes; it does not
  compare them. The `boot.iso` line will differ between runs and that is expected (§5.3a).
- For measured boot, record the expected PCR values with `tools/tpm_expected_pcr.py` and
  compare them against the running system.
