# Build artifacts
*.o
*.d
*.a
*.bin
*.elf
*.raw
*.iso
*.img
kernel.elf
boot.iso
horus.iso
.build.sha
# scratch dir for smoke-repro-sha; removed on success, left behind on a failure
.repro-sha-test/
# the reproducible CI job's scratch files, if you run its steps by hand
first.sha
second.sha
first.kernel
second.kernel

# Rust build output
rust/target/

# newlib cross-toolchain (downloaded/built locally, not project source)
newlib/
*.deb

# rust/Cargo.lock is intentionally tracked for reproducible builds

# Build tools
tools/mkheadered

# Userspace build artifacts
userspace/*.bin
userspace/*.elf
userspace/*.o
userspace/*.raw

# Log and trace files
*.log
qemu.log
*.trace
*.dump
serial.log
debug.log

# Editor temporaries
*~
*.swp
*.swo
*.bak
*.tmp

# IDE
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# OS
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db

# Secrets — never commit these
*.pem
*.key
*.crt
*.p12
id_rsa*
id_ed25519*
*.token
*.secret
.env
.env.local
.env.*.local
secrets/
credentials/
passwords.txt

# Personal notes
NOTES.md
TODO-personal.md
private/
personal/

# ISO staging directories
isofiles/
iso/
build/
dist/

# Windows artifacts
*.exe
*.dll
*.pdb

# AI editor artifacts
.aider*
.claude/
CLAUDE.md
graphify-out/

# System Prompt
system_prompt.txt

# Python
__pycache__/
*.pyc
.venv/
horus_usage.log

# Generated program-image byte arrays for PROC_SELFTEST (execve-from-fd)
userspace/*_image.h

# Generated boot-module hash manifest (tools/gen_module_manifest.sh)
src/kernel/boot_module_manifest.h
tamper.iso

# Soak evidence: full serial logs of failing boots (see smoke-session-smp-soak).
# Kept only when a run fails; uploaded as a CI artifact, never committed.
soak-evidence/

# syscall-coverage serial transcripts (make smoke-syscall-coverage); evidence, never committed
.syscov-evidence/
